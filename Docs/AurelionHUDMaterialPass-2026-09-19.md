# Holographic HUD material pass, 19 September 2026

## Nested legacy readout retirement

`WBP_AurelionGameplayHUD.WBP_WeaponInfo` is now explicitly collapsed in addition to its `VerticalBox_0` parent. The nested child has no self-visibility binding or SetVisibility writer in the exported owned weapon Blueprint. The gameplay HUD graph has a SetHUDHidden loop over direct CanvasPanel_Base children which can restore their visibility; a nested retirement survives a visible parent without altering menus, interaction, weapon data or the holographic surface. This changes the owned HUD asset only, preserving its tree and graph references.

Read-only graph export: `HUDLifecycleAudit-20260919-141107-87e61449`. Direct SetHUDHidden hide/show calls alone **did not reproduce** the later mission failure in controlled run `HUDRestoreRepro-20260919-141648-9bb49ac2`; they left the parent collapsed. The exact full-route restoring caller/timing is therefore not certified. The expanded fixture separately makes the actual legacy parent visible as an explicit UI stress case, not a mission-state or handoff simulation. Old content failed this case in `HUDVisibleParentBefore-20260919-142128-1140ab58` with parent Visible and weapon SelfHitTestInvisible.

`HUDNestedRetire-20260919-142253-46c60fd6` saved the authored nested collapse after compilation and tree-preservation checks. Fresh visible run `HUDVisibleParentAfter-20260919-142403-0bc2369b` passed the same stress case: parent Visible, weapon Collapsed, actual holographic ammo 32/218. Native hostile detection, Blackout contact suppression, and restoration also passed. The actual `hud-firearm-live-contact.png` was visually inspected and contains the HUD with no lower-right ammo duplicate. It is a controlled spawned-enemy editor screenshot, not mission completion or a clean reference-comparison capture. A full late-handoff route replay remains pending.

The fixture's initial non-retained launch exited at `ready`; a subsequent temporary runner used an incorrect keep-alive API; an early stress case tried an unavailable reflected widget property. Those attempts are not acceptance evidence. The corrected fixture locates the actual nested widget through the existing read-only validator's returned path. `Scripts/Validation/Aurelion/run_hud_gameplay_fixture.py` preserves the ExecutePythonScript keep-alive/end-PIE wrapper using EditorPythonScripting. Run it through run-editor-script.ps1 without KeepEntryOpen; the fresh successful run used the equivalent temporary wrapper. The read-only validator now reports both parent and child visibility and recognizes retirement at either level.

Pre-change full baseline: `20260919-140839-d6658baf`, build without SkipBuild and 719 tests. Post-change `20260919-142619-2a7a91a7` passed the build without SkipBuild, all 719 tests, coverage and source integrity; no tracked files changed while it ran. Creator-owned map and grenade assets were excluded, and the M12 map retained its prior SHA256.

## Protagonist nameplate

The plate now contains an Overlay with the existing survival bars and a centered ProtagonistName TextBlock. The text binding reads GetHolographicHUDView.Protagonist, exactly matches Sov.Character.Player.Selene, and selects localized SELENE or TARRIK text. Color and Opacity binds to View.Palette.Accent. The 12-point type uses 1400 letter spacing; the original bar padding remains on PlateBars, so adding the cap does not move the bars. This is authored in the existing UMG asset, with no C++ changes or widget reconstruction.

Normal editor session `HUDIdentity-20260919-100145-6cf19d70` compiled and explicitly saved the Blueprint. M12 PIE showed TARRIK above the shield without overlap; widening the initial 350 spacing to 1400 and binding the palette produced the amber cap in `UserData/Saved/Screenshots/WindowsEditor/ScreenShot00000.png`. The read-only probe retrieved TARRIK from the live TextBlock. Its first attempt used an unavailable TextBlock color getter; the corrected probe reports color as visually inspected rather than claiming an evaluated color equality. Its opaque GameplayTag string is not identity evidence. Selene gameplay, switching and high contrast remain unverified.

Fresh process `HUDIdentityVerify-20260919-102124-bb7bbf2e` exited 0, verified the name widget's type, overlay parent, font settings, all ten native bindings and existing material references, with no compiler ensures or Python errors. Full validation `20260919-102235-58ede5ae` passed the build invocation without SkipBuild, 719 matching automation tests, coverage and source-integrity checks. The pre-change full baseline was `20260919-095940-30c55de2`. These gates do not establish reference fidelity or full HUD/gameplay completion.

Remaining priority HUD work includes functional ability pips, live radar contacts and blackout behavior, both-protagonist runtime validation, high-contrast presentation, and further housing/frame polish. The lethal-floor gameplay cue remains the next handoff priority after HUD visibility. Creator-owned M12 and Cinder Sticky Grenade changes were not staged with this pass.


## Gameplay-driven radar sweep

The saved HUD EventGraph now routes `OnHolographicHUDUpdated.UpdatedView.SweepSeconds` into the RadarDisc dynamic material parameter `SweepSeconds`. The material draws a clockwise beam with a fading sector at 70 degrees/second, matching the native presentation clock. No autonomous material time or decorative enemy contacts were added. The UMG graph compiled and was explicitly saved in `HUDSweepGraph-20260919-093959-d1504fbb`.

Live M12 PIE verification in `radar-sweep-live.json` measured identical view/material values at 36.844929 and 37.875427 seconds. `Tarrik-RadarSweep.png` captures the amber sector in actual play. Fresh saved-asset validation `HUDSweepVerify-20260919-095828-84135d2b` passed the existing ten native bindings, material references, health brush and compass checks. This establishes the sweep connection only; live radar contacts, named cap, functional ability pips, Selene switching, drawn-weapon ammo, high contrast, alternate scales and full reference fidelity remain incomplete.

The first material author attempt (`HUDSweepMaterial-20260919-093843-9c61bc27`) failed its single-expression assertion: the asset contains an unused older custom expression. The corrected script follows the connected emissive graph and saved successfully through the retained editor. The initial live probe used an unavailable Python method spelling; the corrected `get_scalar_parameter_value` probe passed. These failed attempts are not qualifying evidence. Full validation baseline before this change is `20260919-093425-1b849158`. Post-change full validation `20260919-095940-30c55de2` passed the build invocation, all 719 matching automation tests, report coverage and source integrity; it does not establish full visual or gameplay acceptance.

## Reference housing follow-up

Glyph validation: `HUDGlyphVerify-20260919-093307-13c58e89` exited 0, verifying saved material references, ten native bindings and the revised content padding, with no material compile failures, Python errors or ensures. Full validation `20260919-093425-1b849158` passed the build invocation, all 719 matching automation tests, report coverage and source integrity (95 warnings; packaged build not run). Baseline was full passing run `20260919-092119-35559ad7` immediately before this work. These gates do not establish complete HUD reference alignment or character gameplay correctness.

Reference glyph follow-up: `author_hud_reference_glyphs.py` updates the existing custom expressions in `M_SovPlateHousing` and `M_SovAmmoHousing` in place. The plate now has a medical cross and filled shield emblem; the ammo housing has three pointed cartridges with casing seams. PlateBars left padding is 90 and AmmoText padding is (62,8,18,8), reserving space for those symbols beside the native values. No widget tree rebuild, gameplay reader or C++ changes. Author run `HUDGlyphs-20260919-092524-086aa921` exited 0 and retained all ten native bindings, with no material compile failures, Python errors or ensures. Visual and scale acceptance is pending.

Designer review found a retained dark Slate brush tint multiplying the material, which had also dimmed the earlier housing pass. Both housing brushes now use white tint, and PlateRegion/AmmoRegion Brush Color bind to `View.Palette.Accent` through UMG. The normal editor saved these corrections in `HUDGlyphReview-20260919-092633-6121f645`; no material compile failures, Python errors or ensures were logged. The designer shows all three symbols clearly. Actual M12 PIE at 1280x720 shows Tarrik's amber health/shield symbols next to the live bars with compass clearance (`Tarrik-Glyphs.png`, Shot showui). The weapon was stowed, so ammo glyphs are designer-qualified only. Selene switching, ammo values with a drawn weapon, other UI scales and high contrast remain unqualified. Names, functional pips and live radar contacts remain outstanding.

Health/compass validation: fresh editor `HUDHealthVerify-20260919-092013-4b1cbfdd` exited 0, verified the saved health brush references, Mask fill mode, all ten native bindings and active compass offset, with no material compile failures, Python errors or compiler ensures. The unused legacy HUD copy was restored byte-for-byte from its pre-edit backup after editor exit. Full validation `20260919-092119-35559ad7` passed the build invocation, 719 matching automation tests, report coverage and source-integrity check (95 automation warnings; packaged build not run). This is engineering validation, not reference-fidelity or full gameplay acceptance.

Health/compass follow-up: `M_SovHealthBeveledFill` supplies a grayscale energy gradient, bevel highlights and clipped angular ends. Both health brush layers use this shape, with Mask fill mode so depletion clips instead of stretching the ends. HealthBar now binds to `View.Palette.HealthFrom` for the deeper red/teal endpoint. This is not yet a two-endpoint palette gradient or a high-contrast-qualified brush. The existing widget hierarchy and ten native bindings are preserved.

Actual M12 PIE in `HUDHealthReview-20260919-090653-b19a05d6` shows Tarrik's red shaped health fill and, after correcting the active asset, a compass below the plate. `Tarrik-Health-Compass.png` is the `Shot showui` capture. The active frontend creates `/Game/Aurelion/UI/WBP_AurelionGameplayHUD`, confirmed by `live-widget-classes.json`; the controller's legacy GameplayHUDClass points to a different unused project copy. The initial placement attempt on that unused copy was reverted to its original top offset of 20. The active Aurelion compass now has top offset 148, retaining its original width, height, anchor and alignment. No map or C++ changes were made. Additional UI scales, Selene, health depletion in combat and high contrast remain unqualified.

The compass read-only probe initially failed because OverlaySlot does not expose `get_padding` in this engine Python API. It was corrected to read the `padding` property; the subsequent probe completed. This error happened after the health widget save and did not invalidate that saved binding.

Palette follow-up validation: `Saved/Validation/20260919-085830-07151a8b/summary.json` passed the full build invocation, all 719 matching automation tests, report coverage and source-integrity comparison. This qualifies the saved ArcFill/RadarDisc binding changes for this gate; it does not qualify Selene switching, live radar contacts, high contrast or reference fidelity.

Live palette follow-up: ArcFill and RadarDisc now bind Color and Opacity to `View.Palette.Accent` in UMG. Both bindings were compiled and saved in `HUDPalette-20260919-085107-cca6c339`. The actual M12 PIE entry with Tarrik shows warm amber radar/band colors, shield and health filled, and the otherwise open center. `Tarrik-HUD.png` in that run directory was captured using `Shot showui` and includes editor chrome. It is partial visual evidence, not combat or Selene qualification. The existing Narrative compass overlaps the health plate; health currently reads as a flat orange rectangle and needs a beveled gradient closer to the red reference. Ammo was hidden with the weapon stowed. No live radar contacts or ability pips have been authored yet.

The subsequent `author_hud_reference_housings.py` pass replaces the 56-tick arc with eight broad inset cells, a translucent dark body, bevel lines and a central split with chevrons. Radar now has a dark circular backing, three rings, cardinal ticks and a forward-facing player arrow. New `M_SovPlateHousing` and `M_SovAmmoHousing` UI materials supply angular backings without changing the widget tree. Plate padding was adjusted to fit the shield and health bars into those housings.

ShieldBar and HealthBar now have UMG property bindings to `View.Palette.ShieldTo` and `View.Palette.HealthTo`. Both compiled in the designer and were explicitly saved through EditorAssetLibrary; initial toolbar save attempts had not written the asset. Other colors are still fixed and need palette bindings. These two bindings are not yet proven through protagonist switching in PIE.

The normal editor pass is recorded in `Saved/Validation/Aurelion/HUDDesigner-20260919-082919-f568ce0a/ReferenceHousings/result.json`; all ten native bindings still match. Designer inspection confirms the broader arc, backed radar and angular housings render. This is not a gameplay capture or reference-match qualification. The first component-mask connection attempt failed before saving; changing its input to the default unnamed pin resolved it. Intermediate graph reconstruction produced missing-input shader warnings, so a fresh-process load must verify the completed graphs separately.

Remaining reference work includes the named cap, health/shield glyphs, six functional ability pips, actual radar contacts and sweep, full palette/high-contrast handling, frame-edge detail, and proportions/overlap checks at runtime. In the designer, the arc still passes beneath the right side of the radar and the plate needs further contour refinement. The historical pass below describes the earlier state, superseded by this follow-up where noted.

Follow-up validation: fresh editor `HUDHousingVerify-20260919-084805-db8414d8` exited 0, verified all four material references and ten native bindings, and logged no material compile failures, Python errors or compiler ensures. Full validation `20260919-084931-06ec192b` passed the build invocation, all 719 tests and source-integrity check. The preceding `20260919-084615-a04738e3` run passed build/tests but was rejected because verification scripts changed during the run; it is not qualifying evidence. These checks still do not prove live palette switching or visual/gameplay acceptance.

This is an incomplete presentation pass on handoff task 2. It does not qualify the reference match or the 90% visual/gameplay goal.

`WBP_SovHolographicHUD` now uses a procedural UI material for the segmented Echo sweep, driven by the existing native `ArcFill` scalar `Fill`. The opaque rectangular Echo overlay is collapsed. The radar background is a transparent concentric reticle rather than a solid square. The survival bars have thinner shield/stamina strokes around a heavier health stroke, with transparent region backings. Their existing native value bindings and automatic region placement remain intact.

Two self-contained engine materials were added under `/Game/Aurelion/UI/HUD`: `M_SovEchoSegmentedArc` and `M_SovRadarReticle`. Neither references raster artwork or marketplace assets. The radar material contains no contacts; contacts must still be authored from the actual view array.

The first attempted tree rearrangement failed the binding check and was not saved. `AddWidgetToTree` recompiles/reinstances the existing Blueprint; retaining references across these operations produced `TRASH_` widgets and missing bindings. Its log contains compiler GUID ensures. The successful pass changes existing widget properties only. It does not modify C++ or either mission map.

Evidence:

- Baseline full build and all 719 tests: `Saved/Validation/20260919-081402-cbd6b357/summary.json`.
- Authoring and all ten native name/type bindings: `Saved/Validation/Aurelion/HUDMaterialPass-20260919-082402-37739a5e/hud-refinement.json`. Exit 0, no Python errors, material compile failures or compiler ensures in this successful run.
- Fresh editor reload: `Saved/Validation/Aurelion/HUDMaterialVerify-20260919-082505-70a718b4/hud-material-verification.json` passed, confirming both saved UI-material references and all ten native bindings.
- Post-edit validation: `Saved/Validation/20260919-082610-03178650/summary.json` passed the full build invocation (target up to date), all 719 matching automation tests and source-integrity comparison. This does not replace visual or live-data qualification.
- Backups of the original widget are inside each authoring run's `Backups/Content/Aurelion/UI/HUD` directory.

Next: implement the named identity plate, resource labels, ability pips, dynamic protagonist/accessibility palettes and live/memory radar contacts in UMG. Python reflection does not expose the editor binding structures. Use the Blueprint editor for that pass; do not add another runtime HUD owner or substitute decorative contacts. The current fixed colors are temporary, not evidence of either protagonist palette working. Verify both protagonists, Blackout, high contrast and UI scaling in actual PIE with `Shot showui` captures. The transparent backings are not yet suitable for high-contrast mode; its opaque palette binding is required before acceptance.

The lethal-floor cue remains the next handoff priority after HUD completion.

## Supplied visual references

The creator reattached the actual references after the material pass. They are retained as design references at `Art/References/Aurelion/HUD/Tarrik.jpg` and `Selene.jpg`; do not import them as flat HUD textures. Both were visually inspected in the conversation.

The current material pass is too skeletal. The references show dark translucent housings, a substantial beveled health plate, a thin separate shield trough, six inset pip cells, a named top cap, angular ammo housing, a backed circular radar with a directional sweep, and a substantial bottom band split by central chevrons. The arc has broad insets rather than the current 56 fine ticks. Match these silhouettes, stroke weights and proportions before calling this a reference-aligned pass. Tarrik's health is red inside warm gold trim; Selene's health is teal inside pale cyan trim. Preserve the large unobstructed center. Faint framing follows the screen edge; noisy plasma must not overwhelm those clean housings.

## Six gameplay-driven ability cells

`WBP_SovHolographicHUD` now has an AbilityPips Image in the existing plate Overlay, using the new translucent UI material `M_SovAbilityPips`. Its update event refreshes six material parameters from the native View.Pips array. Ready fills the cell, Active adds a bright central stroke, cooldown shows proportional dim fill, and unavailable/locked/non-ready states remain outlined. An explicit array-index guard clears missing slots to prevent stale indicators after a loadout shrinks. Accent follows the native protagonist palette. No C++ edits.

The graph was authored and compiled in the Blueprint editor; `author_hud_ability_pip_cells.py` records the one-time material/tree addition only and must not be treated as graph reconstruction. It refuses to overwrite an existing pip material. The graph is retained in the binary widget asset.

Run `HUDPips-20260919-111743-e069d09b` saved the completed graph. `pips-live.json` passed three native view/material comparisons (NeedsEcho and five missing slots, Tarrik accent). `pips-controlled-states.json` passed all eight enum states plus shrinking/empty arrays through temporary presentation-only View injection, restored in finally. These cases validate presentation, not actual ability activation. The initial probe's read-only property assignment was corrected to editor-property assignment. An intermediate invalid array node failed compile and was replaced with the proper array-function node before save. The authoring run has earlier reconstruction ensures and is not a clean-load qualification.

Actual M12 PIE at 1280x720 shows six inset outlines under the plate with clear name/shield/health separation. This is partial reference progress: housing finish, live radar contacts/Blackout, Selene gameplay and accessibility validation remain outstanding. The first SIE launch failed campaign player initialization; normal Play subsequently succeeded. Baseline full validation was `20260919-111327-f7c31141` (719 passing tests, no SkipBuild). Fresh-load and post-change validation are recorded separately after completion.

Pip follow-up validation: fresh editor `HUDPipsVerify-20260919-113250-6175cb31` exited 0, verified the saved pip Image/material and all ten existing native bindings, and recompiled the Blueprint without errors or ensures. Full post-change run `20260919-113416-93030876` passed the build invocation without SkipBuild (target up to date), all 719 matching automation tests, report coverage and source integrity. This establishes a validated incremental HUD pass; it does not establish the 90% reference/gameplay goal or packaged-build acceptance.

## Radar contact rendering

The saved HUD OnPaint override now iterates the native View.Contacts array and draws each contact at RadarCentre + Offset, centered on a 20 * Scale brush. Live sightings use the filled UI brush; memories use the hollow brush. Tint comes from Palette.HealthTo and contact Alpha. Empty arrays issue no draw calls, avoiding retained contact widgets. This is authored entirely in the widget graph and material assets; no C++ changes.

`author_hud_radar_contact_brushes.py` records the one-time creation of the material, two instances and two Slate brush assets. It refuses to overwrite the existing material. It does not reconstruct the OnPaint graph; that graph is in WBP_SovHolographicHUD. The first authoring attempt used an incompatible SlateBrush image-size type; recovery omitted the unused brush size, as DrawBox supplies the scaled size. Both Select brush references were explicitly assigned in the editor before save and confirmed in the exported graph.

Authoring/PIE run: `HUDContacts-20260919-113647-b13b3361`. In normal M12 PIE, temporary presentation-only contact injection displayed a filled dot at (-45,-35) and a faded hollow mark at (45,-35), relative to the radar center in view coordinates. Both disappeared when the injected array became empty. The bounded probe temporarily disables the frontend refresh tick and restores it with the original View after 45 seconds. Completion marker was written at 11:57:13 before PIE ended. This is synthetic presentation coverage only, not actual enemy acquisition or Blackout gameplay coverage. Direct detection-array injection was rejected because that property is protected; no detection or settings mutation occurred in that failed attempt.

The actual HUD remains flatter and brighter than the supplied reference housings. Reference polish, actual enemy contacts/Blackout, Selene gameplay, ammo, accessibility and packaged verification remain outstanding. This increment does not establish 90% alignment.

Radar follow-up validation: fresh editor `HUDRadarVerify-20260919-115750-a2a95055` exited 0 with saved brush/material/graph checks and all ten existing native bindings passing, without Python/Blueprint errors or compiler ensures. Full post-change run `20260919-115900-c72db7ed` passed the build invocation without SkipBuild (target up to date), all 719 matching automation tests, report coverage and source-integrity checks. The probe gained failure cleanup after visual inspection; the successful visual run used the same contact drawing and 30/45-second timing with normal-path restoration.

## Dark optical housings and consistent luminous trim

`refine_hud_optical_housings.py` updates only the four existing housing/arc/radar custom shaders. Darker, more opaque interiors reduce scenery bleeding through the status plate, radar and Echo cells; narrow luminous rims and soft halos separate the frame from the glass. Existing glyphs, native color tints, Fill and SweepSeconds inputs are preserved. The plate and ammo distance calculations now account for their wide aspect ratios so a narrow top/bottom rim does not become a broad glowing side cap. No widget tree, Blueprint graph, map or C++ changes.

Baseline: `20260919-115900-c72db7ed` passed full build, 719 tests and source integrity before these edits. Authoring `HUDOptics-20260919-120322-2e04bca3` saved all four materials with no Python or shader errors and retained the native parameters. Fresh review `HUDOpticsReview-20260919-120444-84d192c5` passed the existing saved-HUD material/brush/graph/binding checks. The initial visual review exposed anisotropic side trim; it was corrected in that editor and saved, with the same correction incorporated into the authoring script. A second normal M12 PIE review showed the thin status outline and darker radar/arc interiors without the broad plate-end flares. The radar sweep continued to animate. This is actual Tarrik PIE presentation evidence, not Selene, ammo, high-contrast, Blackout or combat acceptance. The review profile completed accessibility setup only in its isolated UserData folder.

The procedural result remains short of the reference's overall finish. Next priorities include real ammo visibility, both protagonist palettes/resources, high-contrast backings and Blackout gameplay qualification. Saved before-material backups and shader text reside in the authoring run; the script refuses unexpected starting shader revisions.

Optics post-change validation `20260919-120936-f49cc524` passed the full build invocation without SkipBuild (target up to date), all 719 matching tests, report coverage and source integrity. This covers the final aspect correction. It does not certify reference fidelity, every weapon readout or both protagonists. Native ammo visibility depends on a wielded magazine weapon; the unarmed/melee entry review is insufficient for ammo acceptance.

## Firearm HUD and actual Blackout integration

`probe_hud_gameplay_fixture.py` runs normal M12 PIE with an isolated profile, selects the already-equipped Cinderline through Narrative's wield API, and compares native clip/reserve values with the holographic view and AmmoText. It spawns a validation Enforcer ahead of the player to exercise actual proximity detection, enables Blackout through the settings API, and requires detection to remain populated while the HUD contact array becomes empty. It then restores settings, checks contacts return, and destroys the fixture enemy. It does not inject HUD contacts, fabricate campaign progress or certify physical weapon-wheel input. The enemy can cause transient combat feedback before its brain stops; no saved character data is changed.

Initial run `HUDGameplayFixture-20260919-121327-2adda269` passed: Cinderline 0/250 matched the view/text; actual detector/view counts were 1/1, then 1/0 under Blackout, then the view returned to 1 after restoration. The visible gameplay HUD displayed the same ammo. Ordinary gameplay reload input then produced 32/218. UI-inclusive normal/Blackout captures and JSON are in that run.

That empty initial magazine exposed a content-order defect: WeaponItem.AddedToInventory calls Reload for newly granted weapons, but IC_Tarrik and IC_Selene listed their firearms before ammunition. `order_protagonist_ammo_before_weapons.py` stably moves each loadout's existing ammo rows ahead of weapons, preserving every row and quantity. These two project-owned data assets are now explicitly allowlisted. Existing saved inventories are not rewritten. The script records original/reordered entries and backs up each asset. No C++ or mission/map edits.

The strengthened fixture requires a full Cinderline clip and conserved total 250 rounds without reload input. Selene's loadout receives the same ordering correction, but her full gameplay handoff and HUD remain separate outstanding acceptance checks.

Fresh process `HUDLoadedMagazine-20260919-121911-2226fc8a` passed the strengthened fixture: native clip/reserve and displayed text were 32 / 218 without reload input; normal detector/view counts were 1/1, Blackout 1/0, and restored presentation 1. Actual Tarrik PIE was visually inspected with the loaded readout. The run retained normal/Blackout UI captures and JSON, restored settings and removed its enemy, and closed cleanly with no logged Python/Blueprint errors or ensures.

Full post-change validation `20260919-122401-67ca6136` passed the build invocation without SkipBuild (target up to date), all 719 matching automation tests, report coverage and source integrity. The preceding optics full validation was the baseline. This qualifies the controlled Tarrik ammo/Blackout path only; Selene gameplay, reference fidelity, accessibility and packaged verification remain open.

## Authored-route verification and Elite Core repair

Attempt `HUDSeleneRoute-20260919-122720-16be4d14` stopped on a real placed-roster defect: E4.Elite's Core matched `spine_03`, which does not exist on the authoritative SK_Fat parasite mesh. `correct_eclipse_core_body.py` now backs up the asset, verifies CATRigSpine1 against both the current appearance mesh and its exported physics bodies, preserves every other zone field, and checks the override survives compilation. Only BP_AurelionElite is saved; the creator's map edits are not saved or replaced.

Fresh process `HUDCoreRoute-20260919-123133-00b63310` confirmed the placed Elite's Core now matches CATRigSpine1. It then timed out because the roster validator still required an obsolete DemoPistol while NPC_AurelionEnforcer explicitly grants DemoRifle. The validator now derives required weapon classes from the authored direct grants, requires at least one for Enforcers, and checks each is equipped. `HUDRouteRoster-20260919-123516-5ef5bc74` subsequently passed the complete observable roster startup contract with no structural failures, inspection errors or pending actors. This is bone compatibility/startup evidence, not an actual Core break or encounter victory.

That route next exposed an outdated HUD gate requiring the removed numeric shield label. The read-only HUD validator now prefers the current player's holographic surface and checks ShieldBar visibility, fraction, and view current/maximum against the native shield component. The old rounded-label check remains for sessions that actually use that surface. Both M12 entry and E2 gates recognize the new fraction qualification. Existing weapon owner, wield, and ammo checks remain intact.

`observe_hud_protagonist_route.py` samples actual HUD identity and all four native bar bindings during a gameplay route, captures each protagonist, and never writes gameplay or presentation state. Partial captures from interrupted route attempts qualify only the observed protagonist; they do not establish Selene acceptance.

`HUDRouteBar-20260919-123823-4a88bb74` passed entry and earned E1 victory, then timed out aiming from the old coarse waypoint roughly ten metres from the relocated secure-route terminal. The input driver's `acquire_hold` now approaches the current actor through its existing navigation/input path, using a stand-off within the authored interaction distance (also bounded by RequestRange for handoff anchors). It preserves actual focus, hold countdown, mission receipts, collision and navigation requirements.

Fresh run `HUDRouteApproach-20260919-124545-17ab8d24` passed entry, the complete placed-roster startup checks and E1 continuation through a ready Selene after one native recovery. Both physical interactions used the new approach and earned their hold receipts. The read-only HUD observer passed for actual BP_SovTarrik and BP_SovSelene: identity and health/shield/stamina/Echo fractions matched. Both health and shield started full; Echo was 25/100. UI-inclusive captures are `hud-route-tarrik.png` and `hud-route-selene.png` in the run. Live viewport review also showed Tarrik's red/gold and Selene's teal/pale-blue palettes, radar contacts, changing ammo and depleted bars during combat. Selene's Staccato showed 4/32 after selection. The Selene handoff and Staccato-selected shield gates both passed `fraction_matches` against her actual native shield component.

This supplies both-protagonist PIE bar evidence for handoff task 2, alongside the previous actual Blackout fixture. It does not establish reference-quality finish or 90% alignment. The large terminal interaction outline seen over Selene's HUD remains a presentation issue. E2 combat stopped after three no-damage flanks: the final report had accurate diagnostic head rays at Enforcer1 but no health reduction, with 2/12 Staccato rounds remaining. The later live frame showed 4/10 after reload and Selene taking damage. Reports are retained under `E1Continuation/route-follow-on/continue_aurelion_e2_input`; E2 and the remainder of the route are not qualified. No damage, progression, pawn position or resource values were fabricated to pass this run.

The saved Selene PNG was inspected and caught the wheel-selection overlay and groom preparation during handoff; it is a diagnostic capture, not a clean beauty shot. The later live viewport review showed the settled hair, teal health/pale shield, Staccato ammo and changing combat bars. Full post-change validation `20260919-125554-2e38becd` passed the build invocation without SkipBuild (target up to date), all 719 matching automation tests, coverage and source integrity; 95 warnings were reported. Baseline was `20260919-122401-67ca6136`. The observed E2 gameplay failure remains open despite the passing automation suite.

### E2 failure narrowed to the driver progress limit

Reviewing the complete shot receipts from `HUDRouteApproach-20260919-124545-17ab8d24` shows Staccato was dealing damage: 12 native damage transactions included fatal hits on Enforcers 4, 2 and 3 and Drone1. The final no-damage stop therefore does not establish a generally broken Staccato attack. The driver's `flanks` counter accumulated across successful hits and kills; only its missed-round counter reset on damage. It could stop a progressing fight after unrelated earlier misses.

`aurelion_shot_progress.py` now resets both counters on a matched native damage receipt. Two consumed rounds without damage still request a flank, and three consecutive failed flanks still stop the driver. Zero-consumption requests do not count as missed rounds. The existing total route/stage deadlines and ammunition behavior are unchanged. The helper is included in E2's source-integrity manifest. Three focused tests pass, including replay of all 26 recorded outcomes and the persistent-miss stopping boundary. Fresh gameplay validation is required before claiming E2 completion.

Fresh `E2ShotProgress-20260919-130017-3376fb3a` passed E1 and the authored Selene handoff, then passed E2 in 79.312 seconds with 17 native damage transactions, confirmed roster victory and both receiver receipts. The E2 report retained unchanged script/asset checks. No weapon or damage content was changed for this result. This supersedes the earlier driver stop as E2 route evidence; it does not qualify every attack/animation or the later encounters. The large terminal rectangle was traced separately to the optional `bInteractableOutlines` accessibility overlay, not to the holographic HUD widget tree.

The same run also passed E3 entry, rescue combat and both full rescue scenes, E4 entry with all seven protected people alive, and E4A's two native Weaver receipts and real Tarrik handoff. E4B stopped at `prepare_frost`: Selene actually moved within 125.36 cm of the frost anchor, but the Elite at approximately (241,20705,-1110) was too far from the heat request at (550,21600,-1130) for the native reach intervals to overlap. No frost window, thermal payoff or Core break was fabricated. E4B remains open.

`observe_lethal_floor_route.py` was attached during E4B to read actual floor/overlay state. It recorded the Elite held at 532 health with M_AurelionPhaseLattice on CharacterMesh0, and Weaver unheld without an overlay. The observer does not toggle floors or apply damage. It attached after E4A, so it cannot qualify that phase's activation/removal, and E4B never resolved to demonstrate removal or poise behavior. Console focus attempts during E4B coincided with ordinary shots into the floor; this is not a pristine driver-only E4B input capture. The earlier E2/E3/E4A receipt evidence is retained separately.

Post-change full validation `20260919-131501-62a1cec0` passed the build without SkipBuild, all 719 matching automation tests, coverage and source-integrity checks (95 warnings). The three focused shot-progress tests passed separately. Baseline was `20260919-125554-2e38becd`. The floor observer finished and unregistered on PIE shutdown without findings. No content assets or C++ were modified in this increment; thermal reach, complete floor-cue acceptance and visual polish remain outstanding.

## Single ammo readout and capture correction

Direct image review found that `HUDRouteApproach-20260919-124545-17ab8d24/hud-route-tarrik.png` captured the editor scene without gameplay UI. It is **not** a Tarrik HUD image and supersedes the earlier description of that file as UI-inclusive evidence. The Selene file does contain gameplay UI, but includes the weapon wheel and groom preparation noted above. The independent resource/identity observations remain binding evidence, not visual-reference acceptance. A screenshot file existing is not evidence that the intended viewport was captured.

Live Windows viewport review in `HUDVisualReview-20260919-132942-492f0989` showed both the upper-right holographic ammo panel and a duplicate legacy weapon/ammo readout at bottom right. The controlled fixture passed actual 32/218 ammo and the detection/Blackout/restoration sequence. It was then stopped before authoring.

`retire_duplicate_aurelion_weapon_readout.py` collapses `VerticalBox_0` in `/Game/Aurelion/UI/WBP_AurelionGameplayHUD` after confirming its only children are the old weapon widget and the already-collapsed old player-vitals widget. The full tree, child graphs, compass, interaction UI and other overlays remain intact. The original asset is backed up in the run directory; no map is saved. This removes duplicate information from the reference layout without rebuilding the holographic surface.

`validate_owned_weapon_hud_readonly.py` now recognizes that collapsed legacy container. It retains current pawn/ASC, owning-player and native inventory stability checks, but validates visible ammo availability, visibility, clip/reserve values and text against the actual mainhand weapon through the holographic surface. It no longer requires hidden legacy text to represent the visible HUD. The controlled HUD fixture requires the retired container and this readback in addition to its existing ammo/Blackout checks. Fresh session and full-validation results follow below; neither establishes both-protagonist visual polish or all accessibility scales.

`HUDSingleReadout-20260919-133510-3b115895` visually confirmed the duplicate was gone after a fresh load, while the holographic panel showed 32/218. The first expanded fixture failed because the now-collapsed legacy widget no longer ticks its MainhandWeapon/OurWeapons cache. Its visible replacement already matched the native inventory. The validator therefore checks that legacy cache only when the legacy presentation is active; in the retired case it requires the actual current-player holographic surface and validates that surface's live ammo data instead. The failed fixture was stopped and its report retained; a fresh process validates the corrected presentation contract. Baseline full validation for this content change was `20260919-132707-97d30220`.

Fresh `HUDSingleVerified-20260919-133746-de81b497` passed the full controlled fixture and owned HUD readback: clip/reserve/text 32/218, legacy container collapsed, normal detector/view counts 1/1, Blackout 1/0, restored view count 1. Live Windows review and direct inspection of `hud-firearm-live-contact.png` both show the actual Tarrik HUD with only the upper-right ammo readout. This PNG contains the PIE viewport and editor chrome; it is valid UI evidence, not a clean fullscreen beauty shot. The scene still shows the flatter housing finish and thin plate that need further reference polish. No Selene handoff or full campaign route was rerun for this increment.

Post-change full validation `20260919-134008-66dbc167` passed the build without SkipBuild, all 719 matching automation tests, coverage and source integrity (95 warnings). The creator's M12 map SHA256 remains `B7CEEAB512272FC80FE1E3B3454B08DF40BF40BC0E065C3C90993B910D6780D5`. The editor was closed after PIE stopped; no map or C++ changes are part of this increment.

## Reference bevels and arc depth

`refine_hud_reference_bevels.py` refines only the connected custom expressions in `M_SovPlateHousing` and `M_SovEchoSegmentedArc`. It preserves material inputs, widget layout and all gameplay bindings. The existing native rectangles remain authoritative for caption clearance. The script guards the previous shader revision and backs up both assets and their previous HLSL in the run directory.

The plate now has chamfered name and pip housings, separate shield-channel trim, a second recessed bevel and a soft perimeter glow. Its bevels and medical/shield symbols derive aspect from UV derivatives instead of assuming a fixed width-to-height ratio. The arc's half-width grows from .108 to .145 inside its existing rectangle, with wider inset cells, a darker cell edge, central light gradient and a small upper highlight. The native Fill value, eight cells, center split and chevrons are retained.

Authoring `HUDReferenceBevels-20260919-134439-d6deab45` saved the two materials and exited 0 without material compilation errors. Baseline full validation is `20260919-134008-66dbc167`. Fresh gameplay visual review and post-change full validation are required before qualifying the new material pass.

Fresh process `HUDBevelReview-20260919-134544-5c26d9a5` loaded the saved materials and passed the actual ammo/Blackout fixture. Live review showed improved arc depth but detached cap/shelf silhouettes. A second material edit outside PIE extended those two housings into the adjacent main geometry; their trim and glyph placement remain inside the same native plate rectangle. The restarted PIE fixture in this run's `FinalSilhouette` directory passed all ten saved native widget binding/reference checks, the owned live HUD readback, actual 32/218 ammo, detector/view 1/1, Blackout 1/0 and restoration to one contact. Direct inspection of its `hud-firearm-live-contact.png` shows the final connected housings and broader illuminated arc in actual Tarrik play. It includes editor chrome and a controlled spawned Enforcer, and is not evidence of mission progression or a clean promotional capture.

The final silhouette was reviewed in a restarted PIE world in the retained editor, not a second fresh editor process. Neither this pass nor its grayscale material math establishes Selene visual acceptance, high-contrast/alternate-scale acceptance, complete reference fidelity or the broader character/mission goal. No widget Blueprint, native layout, map or C++ changes were made.

Post-change full validation `20260919-135030-3413be81` passed the build without SkipBuild, all 719 matching automation tests, coverage and source-integrity checks (95 warnings). The creator-owned M12 map hash remained unchanged. This qualifies the documented material increment and its tested HUD behavior only; the broader goal remains active.
# Later handoff regression observed

During fresh full mission run `PhaseCueRoute-20260919-135454-4075817c`, actual Tarrik and Selene resource fractions and name bindings passed the read-only HUD observer. Live Unreal window review confirmed the gold/red and cyan/teal HUDs during ordinary gameplay. However, after the later E4A-to-E4B Tarrik handoff, the legacy lower-right `Cinderline` ammo display was visible again alongside the holographic upper-right readout. The initial collapsed-parent fixture does not cover this later lifecycle. The cause is not established; keep this duplicate-readout issue open and verify the handoff path when fixing it. These editor-window observations are diagnostic, not clean reference-comparison captures.
