# Source engineering closure and adversarial-audit handoff

Authority: Sovereign Call: Origins TDD v2.0, 14 August 2026, plus [approved exceptions](CampaignV2ChangeLog.md). The December attachment remains superseded. Baseline: `0378febff90b2cb04ac7887812ba4efe1b87072d`; branch: `codex/source-engineering-closure`.

This pass implements the outstanding native integrations identified in the preceding audit. **Implemented source is not verified UE5.7 behavior.** The creator has explicitly deferred engine compilation until Monday. UHT/UBT, engine automation, content validation, packaged playthroughs, performance and physical devices have not been exercised here. Editor authoring is excluded; no missing Blueprint is presented as inspected or proven correct.

Delivery evidence: [exact files](SourceEngineeringClosureFiles.md), [validation and review findings](SourceEngineeringClosureValidation.md), [executed portable log](SourceEngineeringClosurePortableTests.txt). Detailed contracts: [cinematic inventory](CinematicInventoryTransactions.md), [Mass](CampaignMassEngineering.md), [dialogue narration](DialogueNarrationEngineering.md), [frontend](NativeAccessibilityFrontendClosure.md), [platform/cloud](PlatformServicesEngineering.md), [hearing audio](HearingAudioSettingsEngineering.md), [HDR/output](PlatformOutputEngineering.md).

## What remained and what changed

`P` denotes `Source/ProjectVelkorran`; `N` denotes `Plugins/Narrativeed3f9374a6eV6/Source/NarrativeArsenal`. The complete changed-file inventory and executed checks accompany this report.

| Order / gap | What existed; TDD requirement | Decision and implemented source | Dependencies / change risk |
| --- | --- | --- | --- |
| 1. Cinematic inventory and weapon postconditions | Managed sequences already committed journal, transforms, wield state and native transit. Narrative inventory mutations did not have an equivalent reversible, identity-scoped final-state contract. Full playback and skip must reach the same authored state. | **Extend** `P/Cinematics/SovCampaignCinematicComponent`, `SovCinematicInventoryTransactions` and `N/Items/NarrativeCinematicTransaction`. Native grant/remove/equip/holster/draw plans use stable item identities, exact quantities, membership/state/equipment/wield revisions and explicit per-class permission. Completion and skip share the same ordered writes. Native item callbacks publish revisions before external callbacks; final detachment does not repeat an overridable removal predicate. | Existing inventory/equipment, item visual loading, actual sequence participant and committed-beat receipts. **High:** re-entrant item callbacks, full capacity, later same-value ownership, async visual replacement, rollback after partial progress. Unsupported arbitrary Blueprint side effects remain outside the transaction. |
| 2. Campaign participants in Mass C/D | Narrative pedestrian Mass and project A/B coordination existed, but a pedestrian seed was not a campaign NPC snapshot. Tiers must retain campaign identity/state and avoid duplicate full actors. | **Extend** the encounter director and existing Narrative representation owner/receipt. Eligible NPCs transfer through a validated live encounter record into a Mass entity with a poseable/static proxy; promotion reconstructs a fresh NPC, restores and verifies state, then releases owned suspension. A route fragment supplies bounded authored C-tier presentation movement; D does not simulate combat. | Existing save record, GUID lookup, GAS/resources, equipment/sever/corruption/weak-point owners, actual Mass representation and Narrative animation blend. **Very high:** effect ownership, avatar/controller replacement, pooled proxies, deferred entity destruction and required/future-wave leases. Incompatible actors stay A/B; they are never silently stripped of unsupported state. Performance acceptance still requires measurements. |
| 3. Accessible choices and actual narration completion | Tales reply delegates and accessible CommonUI button names existed without a complete native choice presenter or pressure lifecycle. The timer must wait for readable presentation and supported reader completion. | **Extend** Tales with reply ownership/revisions and authored pressure/silence rules; add `P/UI/Dialogue/SovDialoguePresentationComponent`, native choice widgets and `P/Accessibility/SovAccessibleNarrationSubsystem`. Win64 uses the engine TextToSpeech backend with actual completion/cancellation. Unsupported narration leaves manual choices available and does not invent an announcement-completion timeout. | Existing Tales graph/selection and CommonUI Game layer, localized labels and supported platform voice. **High:** synchronous or off-thread completion, replacement choices, suspended scenes, re-entrant cancellation and stale selection. This is an in-game narrator, not proof of completion from an arbitrary external OS reader. |
| 4. Native frontend consumers and first-boot flow | Settings values, focus/navigation, cues and accessibility requests existed, but many values lacked a source-owned visible consumer in this content-free checkout. Accessibility must be available before the opening scene. | **Extend** existing CommonUI/HUD ownership with `P/UI/SovNativeGameplayHUD`, `SovFrontendComponent`, native settings, subtitle/caption/outline/navigation presentation and acquired-evidence/current-scene review. Real Tales, cue, interaction, navigation and damage producers feed the frontend. Device-local preferences remain separate from the eleven-byte gameplay export. Evidence explicitly separates authored facts/interpretation; legacy summaries are not relabeled as established facts. | PlayerController/local player, existing HUD layers, current protagonist knowledge, real node/scene ownership, source settings and font/localization assets. **High:** first-boot ordering, paused initialization, subtitle backlog, focus/modal lifetime, stale protagonist evidence and large-text collision. Native defaults are functional presentation, not final visual/accessibility certification. |
| 5. Account callbacks and cloud conflict review | Two-bank local saves and account namespaces existed, but configured service callbacks/conflict handling did not. Offline play must continue; account changes and cloud conflicts must not silently replace a campaign. | **Extend** `P/Save/SovSaveSubsystem` and add `P/Platform/SovPlatformServicesSubsystem` with a configured Unreal OnlineSubsystem adapter. Cloud requires explicit session opt-in and a reviewed slot/choice; publication creates immutable revisions with exact readback, not an overwrite race. Import retains reviewed archives and the previous local copy through the existing bank commit. A validated, hash-only two-bank profile hint preserves the last local namespace on offline restart. | Approved/configured OSS identity and user-cloud provider, actual local storage, safe front-end boundary and live operation receipts. **Very high:** account changes, unknown/offline identity, old callbacks, provider failures, concurrent writers and re-entrant UI. No provider provisioning, login, remote upload, deletion or certification occurred during this pass. |
| 6. Complete HDR preview ownership | Peak-output preview and config masking existed, but black-floor/paper-white/UI calibration and monitor identity did not. A preview must not confirm after moving to another physical display. | **Extend** `P/Settings/SovGameUserSettings`, `SovDisplayCalibration`, and `P/Feedback/SovPlatformOutputTypes`. Validated renderer controls cover black floor, scene gray/paper-white and UI white; preview owns its receipt, deadline, observed monitor/topology and display-change subscription. Partial writes roll back still-owned fields, preserving later console overrides. Confirmed config is restored even when hardware disappears. | Actual writable HDR renderer CVars, compatible UI compositor, Slate/native window and unambiguous monitor identity. **High:** priority overrides, partial application, identical-capability monitors, unplug/replug, timeouts and unrelated settings saves. Unsupported output rejects the preview; no physical luminance is inferred from a passing policy test. |
| 7. Missing hearing-setting consumers | Narrative had master, dialogue, music, UI and SFX gain; remaining channels/presets could otherwise be inert controls. | **Extend** existing Narrative sound settings/mix application for ambience, tinnitus-like tones and authored dynamic-range presets. Native settings expose those actual consumers. `P/Narrative/SovNarrativeCueComponent` adds optional controller-audio gain and engine `ControllerFallbackToSpeaker` class routing before playback, retaining caption production and ordinary routing for unconfigured/invalid classes. | Authored sound classes/mixes, real world audio device and target audio backend. **Medium/high:** late device creation, changing devices, modifier reference counts, class routing and missing assets. Controller routing is an engine request, not confirmation that a proprietary device emitted sound. |

The approved combat rosters, finite Cinderline ammo, delayed player Health recharge, deterministic damage and transient sustain-drop rules were preserved. This pass does not add a second ability system, save format owner, inventory, menu input router or free protagonist switcher.

## Vertical slices and acceptance

The source work follows the order above, with independent owners implemented in parallel. Every slice uses an existing runtime producer and an existing authority; none is complete merely because a settings field or Blueprint event exists.

| Slice | Source definition of done | Required validation / likely failures |
| --- | --- | --- |
| Sequence-to-gameplay inventory continuity | Shared playback/skip plan, explicit item opt-in, exact ownership journal, native presentation completion and rollback protection. | Native full removal/grant/equip rollback tests; re-entrant permission/state callbacks; full inventory; transformed weapon replacement; destroyed participant; competing writes; first-boot scene prerequisite. Then actual sequence/weapon assets. |
| Scalable encounter round-trip | Existing director owns the record and identity; only compatible NPCs demote; existing Mass receipts own each proxy; promotion verifies the new NPC before activation. | Production policy boundaries plus real entity/actor receipt and fresh-NPC round-trip fixtures; severed/equipped actors, timed and item-owned effects, missing assets, lost controller/ASC, restore and teardown. Then C/D frame/memory/pose measurements. |
| Read and choose without deadline traps | Native choices display actual Tales replies; selection uses the current reply revision; readable pressure waits for real supported narration; interruption/replacement retires old work. | Real Tales/native widget and narrator adapter tests; slow reading, disabled/extended timers, pause, same-instance replacement, synchronous and worker-thread completion, unavailable voice and authored silence validation. Then keyboard/controller/voice tests on target builds. |
| Operable native frontend | Settings immediately persist/apply; first-boot flow precedes managed opening playback; real events produce captions/subtitles/assist markers; evidence follows current knowledge; scene history is not a cross-scene transcript. | Native settings/control/pagination/Unicode/palette/privacy and producer-binding tests; cue/dialogue replacement, avatar change, teardown, modal focus, no viewport. Then large-text, localized, ultrawide and safe-area visual testing. |
| Optional cloud without losing offline saves | Stable namespace and offline restart; safe account boundary; explicit conflict review; immutable remote revision; durable exact reviewed-copy import; obsolete callbacks cannot consume successor operations. | Real stored-byte restart/import and fake-provider boundary tests using production subsystem logic; no login/provider, different account, cancelled/timed-out/synchronous callbacks, denied/torn writes and re-entrant review listeners. Then approved real-provider fault/concurrency tests. |
| Safe display and hearing controls | Actual renderer/mix/component consumers, missing-capability rejection, owned rollback/cleanup, physical display identity scoped to the preview and controller-only cue routing disallowed. | Production display/math checks; output adapter tests; changed monitor, hotplug, partial CVar write, external override, missing mix/device and audio routing contract. Then real HDR/SDR monitors, sound mixes and supported controller devices. |

## What still requires engineering evidence

1. **Monday's UE5.7 integration gate.** Run the checked-in `Scripts/Validate-Unreal.ps1` against the full project and required plugins, fix UHT/UBT/API/link errors, and run all registered engine suites. Source inspection and portable C++ tests do not establish engine compatibility.
2. **Packaged campaign and recovery.** Exercise actual M01/M02 protected beats and Lyric extraction, M12/M13 dual-hero continuity, retries, legal-boundary saves, death/companion failure, process termination, streaming and partition traversal. No binary maps/assets are available here; an asset-free fixture is not a campaign playthrough.
3. **Target-service/device integration.** Configure the approved OSS provider and author sound/mix assets; verify its callback/cloud behavior, physical HDR/SDR devices, narration voices, external assistive technologies and controller audio. Private platform SDK extensions and certification cannot be manufactured from public source. Optional cloud stays off until explicitly enabled.
4. **Measured performance and content compatibility.** Measure real A/B/C/D budgets, streaming, animation transitions and long-session stability. Add narrowly owned adapters where actual custom item/effect/animation implementations fail safe admission. Do not weaken the admission checks to hide an unsupported actor.

There is no claim that arbitrary Sequencer Blueprint side effects, custom physics/projectiles or third-party item callbacks can be generically rolled back. Future shipped save-format migrations still require genuine historical fixtures; no fictional legacy v2 campaign format was introduced.

## Next three recommended slices

1. **Adversarial source audit of this branch:** attack ownership/re-entry, save/cloud account boundaries, Mass promotion/destruction, cinematic partial rollback, first-boot/readability and unsupported fallbacks. Retain every concrete finding as a regression and fix its existing owner.
2. **UE5.7 build and regression closure:** compile the full project on Monday, run the complete report-checked automation/validation pipeline, and fix actual engine integration errors before promoting the branch.
3. **Packaged opening/convergence recovery and target devices:** automate the real campaign fixtures, fault-inject save/travel/cloud boundaries, and retain timing/performance/accessibility/device evidence.

This is the handoff for a source-level adversarial audit, not a shippability or zero-defect certificate.

## Subsequent console pass

The merged closure was followed by a source compatibility pass for Xbox Series X|S and the PS5 family. See [ConsoleCompatibilityPass-2026-09-05.md](ConsoleCompatibilityPass-2026-09-05.md) for the source fixes, newly identified console build blockers, regression evidence and licensed-engine/device handoff. The counts above describe this earlier closure snapshot.
