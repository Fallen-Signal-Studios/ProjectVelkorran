# Floor lifecycle mission replay

Run directory: `Saved/Validation/Aurelion/FloorLifecycleRoute-20260919-151656-ebd57345`. This retained session uses content at `9f36bda5`, with expanded read-only cue diagnostics and a camera trace. PID 41120 remains the same session; no restart occurred. The camera audit initially failed before gameplay because of an unexposed parent property and a UTF-16 export, then completed after correction. The mission was started in that same editor through its console.

E1, E2, E3 entry, E3 rescue, E4 entry and E4A passed. E4B passed at approximately 63.31 seconds with Tarrik still alive at 68.33 health. The native thermal receipt has attempt `79A986DA4BDA129122B61F9A6F4DC63C`, frost application `FDADFBC141E01B5A34A8DE98A06597FE`, heat `E63C229F40AC1007455791986B314916` and payoff `9C89E9354D6B44BBCB73BC802246A824`. The conventional Core follow-up and combat earned native encounter victory with all protected people alive. This is one successful replay, not a repeatability claim.

The subsequent M13 entry continuation passed: full quarantine/recognition scenes, M12 completion, CP6, durable travel checkpoint, and native world replacement retained the actual journal, identities, inventory/magazines and resource evidence. The M13 gameplay/departure continuation is still running at the time of this note; do not infer its completion or checkpoint reload coverage.

The minimap retirement now has actual later-handoff evidence: at observer time 343.953 seconds, Tarrik's legacy weapon container and minimap wrapper were both Visible while their retired children remained Collapsed. No HUD retirement finding had been recorded by M13 entry. Observers are still active, so terminal cleanup remains pending for this run.

The Weaver's overlay gap reproduced while its floor remained held: the same visible mesh lacked an overlay at 541.968 seconds despite the cue actor still existing with its recovery timer active and an OverlayMID. Protection cleared at 543.468. Thus cue deletion or timer cancellation does not explain this sample. Further inspection of the mesh assignment/clearing paths is required; presentation is not accepted. The Elite's E4B lattice stayed present in the sampled transitions until protection cleared at 579.468 after thermal completion.

Before this run, `FloorObserverDiagnostics-20260919-151525-040f821b` passed the nine-stage controlled overlay coexistence probe with the expanded observer attached. The observer successfully read cue ownership, active recovery timers, material instances and mesh paths without findings. Its JSON remained `observing` because that short fixture released its process keep-alive immediately after ending play; that run does not claim observer teardown coverage. No new cue content has been authored in this diagnostic increment.

## Terminal route result

The full input chain passed. M13 earned all thirteen receipts through SeparateDepartures at approximately 282.53 seconds, retaining the 22 M12 receipts and checking independent assents/handoffs, native co-action arrival, paired lift, complete scenes, evidence provenance and CP7/8/9. Explicit checkpoint reload and rendered quality remain unqualified. PIE was then explicitly ended before any camera content was edited.

HUD retirement and camera observers finalized without findings. The floor observer terminated with a cleanup error involving a destroyed NarrativeAbilitySystemComponent wrapper: Unreal reported `Cannot nativize 'NarrativeAbilitySystemComponent' as 'Object'`, a different marshaling message from the earlier `ObjectInstance is null` case. Its recorded M12 floor transitions remain diagnostic evidence, but its terminal status is not a clean observation pass. Cleanup now recognizes both specific stale-wrapper forms; world-travel cleanup with this revision still requires fresh runtime verification. No original reports were rewritten.

A focused Python regression exercised the actual cleanup function with each stale-wrapper error followed by a live delegate. Both paths preserved removal of the live delegate, cleared retained bindings and finalized the report. This is a narrow regression check, not fresh Unreal world-travel acceptance. Full validation after the diagnostic and camera-content changes is `20260919-154256-b1ec2a17`: build without SkipBuild, 719 tests, coverage and source integrity passed.
