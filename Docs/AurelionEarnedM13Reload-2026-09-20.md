# Latest earned M13 checkpoint round-trip

Scope: public CP9 restoration after the unpaced cinematic startup repair route. This does not qualify companion combat, new player inputs, all artwork, or the overall TDD goal.

`Scripts/Validation/Aurelion/check_earned_m13_reload_roundtrip.py` loads the unchanged banks from `CinematicStartupUnpaced-20260920-213727-1184bfea`, whose actual ordinary-input route earned all 35 journal rows and the final departure checkpoint. No serialized payload is decoded or rewritten.

The first load compares current journal/evidence, player-state and companion stable identities, inventory/ammunition, resources and exit positions directly with the earned route. The existing native checkpoint verifier also checks canonical mission facts, active companion admission and completed lift state. Four seconds of stable state are required. The second public load additionally verifies new native world/controller/game-mode identities and a distinct load request token, plus unchanged complete snapshots. Both loads check one live HUD, correct Tarrik identity/health binding and released movement/look input. Screenshots require separate visual inspection.

The first run, `EarnedM13ReloadRoundtrip-20260920-214855-8c1ef171`, completed the first load and its four-second state/HUD checks. Its second load succeeded natively but the checker rejected generation 21 after expecting the original generation 20. No gameplay pass is claimed for that run.

Readback of `ASovAurelionCheckpoint::HandleOverlap`, `Tick` and `HandleStateRestored` confirms that restoring inside the physical CP9 volume deliberately re-arms its one-write-per-visit capture. The revised checker records successful native CP9 save callbacks and requires each refresh to preserve the exact earned journal/evidence. Before its second request, it reads the current public slot header, admits only an observed refresh and requires the completion callback to match that exact header. It does not disable checkpoint capture or ignore generation differences.

`EarnedM13ReloadVerified-20260920-215224-cdf78888` observed the successful CP9 checkpoint refresh and unchanged earned journal/evidence. Its immediate second-world check rejected a nonzero companion velocity before four-second stability could be established. That run remains failed; it does not prove lasting departure drift.

The final probe records any settling velocities/positions, requires the earned exit positions within the existing six-centimetre tolerance on every sample, and requires four consecutive seconds below the existing five-centimetre-per-second settled threshold. Transient settling is allowed for at most fifteen seconds. The exact native world/token and full snapshot checks remain in place. Checkpoint refresh observations are filtered to checkpoint slot zero, excluding a separate automatic slot publication with the same boundary name.

Settling verification: `EarnedM13ReloadSettled-20260920-215443-2d4ea8b5` passed both public loads and all semantic/input/HUD checks. Both maps remained byte-identical. The first load used earned generation 20; the second used witnessed checkpoint generation 21. Each settled for at least four seconds. Three early samples showed only vertical companion velocity, with no horizontal displacement; the largest sampled departure height difference was under two centimetres. Stable snapshots subsequently passed the full native verifier. This is transient capsule settling, not sustained follow drift in this test.

Both `reload-0.png` and `reload-1.png` were inspected: one amber Tarrik HUD, correct identity and full health/shield, radar and Echo arc, no duplicated HUD or leftover cinematic overlay. The images also show that Tarrik's dark armor remains difficult to read against the departure floor; protagonist lighting is still a visual refinement item. These captures do not qualify Selene's face shading or companion combat.

No production C++ or content changed in this pass. The reusable observer and this report are the deliverables. Full post-change repository gate `Saved/Validation/20260920-215632-074058ad` passed its build, all 726 matching tests, report coverage and source integrity (99 warnings). The pre-change baseline was the passing full build and 726 tests at `20260920-213451-005b43d0`. Neither used SkipBuild. All three probe processes and the final validation process are terminal.
