# Project failure-menu footer polish, 6 September 2026

The playable failure screen uses `/Game/UI/Narrative/Menus/Fail/W_NarrativeMenu_Failed` through the project controller's `DeathMenuClass`. Only `CommonTextBlock_CornerText`, containing the original developer instructions, is collapsed. The header, failure message, Respawn and Quit buttons, graph events, bindings, slot geometry, and fade animation remain authored as before.

Reproduce after the project framework and controller UI setup, with PIE stopped: run `Scripts/Editor/setup_project_failure_menu_footer.py`, then `Scripts/Editor/verify_controller_ui_remap_readonly.py` through Unreal's Python console or startup script. Both require the built `ProjectVelkorranEditor` helper. Reports and official read-only text exports go to `Saved/Validation/WorkPCSetup`; set `VELKORRAN_SETUP_OUTPUT` for an alternate output directory and use the same directory for both scripts. Replay this step after recreating the controller from its source defaults. It saves only the project failure menu and project controller; it does not change plugin templates.

The helper verifies the exact full footer text and widget ownership, refuses graph/visibility-bound footers, checks the latest controller UI remap and its class map, and compiles both project packages with zero errors before saving. The original failure menu, Narrative player controller and shared `WBP_NarrativeMenu` parent retain their persistent object fingerprints, dirty flags and disk hashes. The failure menu uses native `NarrativePlayerController` casts and the existing shared menu parent; no additional UI/controller class remapping is required.

The first guarded execution withheld saves when Unreal changed four `FadeIn` animation object fingerprints. Official before/after exports proved that only their `Signature` GUIDs changed. `UMovieSceneSignedObject::Modify()` regenerates these revision IDs for compilation/cache invalidation. The guard now permits only those exact four object paths/classes after checking that every non-Signature property is byte-for-byte identical. Animation keys, times, bindings and evaluation data remain checked; widgets, graphs and layout are never exempted. The same-editor repeat succeeded with zero errors and zero warnings for both packages and confirmed the already-collapsed footer, demonstrating idempotence.

The supplemental report contract is `schema_version: 1`, `success: true`, and one exact `additional_ui_pairs` entry mapping the stock failure menu to its project copy. The verifier admits that pair only with successful source preservation, saved target packages and zero-error compilation evidence. It retains all 11 controller-default checks and all previous 24 Blueprint checks, then checks the new failure menu's parent, compiled inheritance and graph references. The verified run inspected 25 Blueprints, 362 graphs and 3,967 nodes with no residual old-controller references or errors. The new failure menu contributed nine graphs and 89 nodes.

Evidence: `project-failure-menu-footer-setup.json`, `controller-ui-remap-verification.json`, the first guarded report, and official animation text exports. The source manifest records the successful report hashes and the additional package. This is a content overlay for the existing full project; the original Narrative menu parent, button widgets, styles and main-menu asset remain baseline plugin dependencies. Live visual acceptance passed in GUI12 on 6 September 2026: after Selene died at 17:52:03 UTC (10:52:03 PDT), the failure screen observed at 17:52:40 UTC (10:52:40 PDT) had no developer footer and retained its OH DEAR header, message, Respawn and Quit buttons. Clicking Respawn at 17:53:40 UTC (10:53:40 PDT) returned Selene alive with 100 health and 100 shield, and normal Hound attacks resumed. This was a live UI observation; no screenshot file is claimed.
