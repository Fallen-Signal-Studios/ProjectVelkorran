# Aurelion complete architectural rebuild

The active goal is to replace the grayboxed Aurelion level with a custom Blender-authored architectural kit, at the user's requested highly detailed AAA quality bar. This covers both M12 and M13, every playable room, transitions, vistas and cinematic backgrounds. Existing first-pass cladding and shuttle/vault assets are candidates for improvement, not final-quality acceptance. Supporting-cast work and the broader 90% TDD alignment objective remain active.

## Art direction and kit coverage

The August TDD's King-era inheritance and layout reference pages 21–24 govern the art: ivory stone, functional gold, deep architectural layering, responsive geometry and light conveying information. The dimensioned layout governs playable space. Corruption modifies this architecture without replacing it with an unrelated biome.

The kit must cover straight and corner wall bays; piers and buttresses; door/open passage surrounds; arches and vaults; cornices and capitals; floors, borders and threshold transitions; stairs and landings; balconies, railings and parapets; refuge interiors; terminal/briefing focal structures; exterior/dock structures; localized damaged and Eclipse-overlaid variants. Repetition, scale and seams must be reviewed as assemblies, not only as isolated models.

## Acceptance requirements

- Review silhouette, layered construction, edge highlights, surface detail and purposeful ornament against the reference in neutral lighting and production Unreal lighting.
- Author editable Blender sources, stable module dimensions, bottom/grid pivots, clean normals, material slots, UVs with consistent texel scale, appropriate collision and LOD/Nanite configuration. Baked/tiled surface maps must survive import; Blender procedural preview shaders alone do not qualify.
- Replace the visible graybox throughout both maps. Retained invisible gameplay proxies must have a documented purpose and verified correspondence with the final visible architecture. Preserve required route dimensions, cover, wall-run surfaces, interaction reach, camera composition and encounter readability while making deliberate geometry improvements where needed.
- Inspect every room at player height, in combat and in its cinematic views. Fix seams, floating pieces, intersection, repetition, material scale, exposure and missing rear/upper surfaces.
- Verify both campaign priority routes, companion/enemy movement, saves and representative packaged GPU/memory performance after the rebuild.
- Do not claim AAA acceptance or 90% alignment from polygon counts, asset import, a studio render or a partial room pass.

## First detailed module family

`Art/Source/Aurelion/build_architecture_kit.py` creates an editable assembly and separate FBX modules under `ArchitectureKit`: a four-metre/seven-metre wall bay, a seven-metre layered pier, a four-metre stepped cornice and a four-metre inlaid floor. The bay adds fitted masonry, deep oblique reveals, incised plaques, service louvers and a concentric mechanism register. This is a source prototype for reviewing the architectural language; it is not the completed kit or map replacement.

The existing 63.75% slice estimate is unchanged. Final surface authoring, module variants, campaign deployment and runtime qualification remain pending.

## Unreal integration

The first four modules are imported under `/Game/Aurelion/Environment/ArchitectureKit/Meshes`. UV0 now uses one texture tile per metre on dominant face planes; UV1 retains unique packed islands for lightmapping. The Blender FBX round-trip checks pass with two UV channels. Unreal import checks centimetre dimensions, three material slots, source-file identity and Nanite configuration. Solid bay, pier and floor assets have one generated box collision each; the ornamental cornice has none. These initial collision envelopes still need gameplay-context review before deployment.

Owned material copies under `ArchitectureKit/Materials` provide ivory stone, gold and dark reveals without changing existing campaign materials. The stone uses retained base-color/normal/roughness textures, restrained base-color variation, a 0.2 normal blend and roughness of 0.34 plus 0.2 times its roughness texture. This is a material candidate, not final surface acceptance.

`/Game/Aurelion/ArtReview/L_Aurelion_ArchitectureKit` contains a separate two-bay assembly and review camera. The imported detailed face is local +Y; review placement uses yaw 180 to face the camera. Initial backside captures were rejected and the orientation corrected. No M12/M13 actors or mission progression were changed by this import.

Import evidence: `Saved/Validation/Aurelion/ArchitectureKitImport-20260913-181657-2f21bf4d/kit-import.json`. The initial multi-view capture suffered overlapping screenshot tasks and does not establish distinct player/detail review. The capture helper now waits for task completion, pilots the review camera and verifies both imported UV channels. The runner accepts the dedicated review map for subsequent art iterations; retained gameplay-route runs still require M12.

The completed review is `Saved/Validation/Aurelion/ArchitectureKitThreeViews-20260913-182520-73516938`, with separate assembly, player-height and detail PNGs and `capture-complete.json`. A callback reentrancy guard was also required because screenshot preparation pumps editor ticks. All three images were inspected. Geometry and material assignments survive import, but prominent shadow noise remains in the offscreen captures. These images establish assembly/detail review, not final lighting or surface-quality acceptance.

Next art requirements include plain and mechanism-bearing bay variants, deeper joints and focal construction, controlled surface wear, continuous ring joints, corner/portal assemblies, and fitting the family into the actual rooms. Repeating the same concentric register on every bay is not the intended final architectural language. Production-lighting review, collision fit, Nanite fallback/platform behavior and measured GPU cost remain unqualified.

## Plain bay and portal expansion

The kit now has six modules. `SM_Aurelion_KIT_WallPlain_4x7` retains the construction and channels without the concentric register, allowing mechanism-bearing bays to be used selectively. Closed ring mouldings now share their wraparound topology instead of using overlapping end caps; this addresses the seam observed in the previous close-up.

`SM_Aurelion_KIT_Portal_6m` provides an eight-metre-wide, 7.8-metre-high frame with a nominal six-metre lower opening, layered stone, recessed dark channels, gold rails and stepped feet/collars. Six authored convex hulls follow its frame segments rather than filling its aperture. The Blender FBX round-trip validates all six modules and checks nine clear rays through the portal plus blocked rays at both sides. These checks are source-art evidence, not gameplay traversal.

The Unreal refresh in `Saved/Validation/Aurelion/ArchitectureKitExpansion-20260913-183123-7e564b07` imported both new modules and refreshed the owned existing meshes, preserving the review map. The importer verifies existing import-source ownership before replacement. The frame imported at approximately 800 x 130 x 779.85 cm with all six convex hulls. Its collision has no generated solid box across the opening. The separate review scene now pairs plain/mechanism bays and includes the portal. Neither campaign map has been refitted yet.

`Saved/Validation/Aurelion/ArchitecturePortalReview-20260913-183309-0bfe3cf6/capture-complete.json` records nine clear Unreal capsule sweeps through the aperture and two blocked side-frame controls, using a 42 cm radius and 88 cm half-height Pawn profile. Four rendered views were produced. The portal and revised ring detail were inspected: the frame silhouette reads, but its upper span still needs richer construction detail and the review lighting is too dark for final surface acceptance. The module remains a structural art prototype, not a final AAA doorway. These editor queries do not prove navigation, live player movement, companion movement or mission-route acceptance.

## Z01 lower-wall deployment

The first campaign deployment replaces the visible lower side walls of M12 Z01 with 28 plain bays and 26 piers at unit scale. This is a partial room pass, not completion of Z01 or the full rebuild. Roof, floors, end walls/doors, ramps, cover, transitions and production lighting still require the custom architecture pass. The existing 63.75% evidence-based slice estimate remains unchanged.

The current-room census is `Saved/Validation/Aurelion/Z01ArchitectureFit-20260913-183815-a2af11a7/z01-current.json`. The existing visible enclosure is a single mesh covering the lower walls and upper shell. Two unsaved cladding trials were rejected because the new panels intersected that enclosure. Eighty-four complex traces established the actual side-wall surface positions; hidden blockout bounds alone were insufficient.

`derive_z01_upper_enclosure.py` creates an editable Blender/FBX derivative of the existing enclosure above world Z=695 cm, retaining its four material assignments. This upper shell is temporary retained architecture, not newly authored final kit content. The original enclosure is hidden visually while its collision remains enabled. The new upper shell and 54 lower-wall modules have collision disabled, preserving the existing physical enclosure and hidden gameplay proxies. Visual/collision correspondence still requires live wall-contact and companion traversal review.

The imported upper derivative has 2,732 triangles. An FBX import warning was traced specifically to malformed normals on the original UCX collision object, which is excluded from the derivative export; it did not identify a visual-mesh normal or UV failure. Sources are under `Art/Source/Aurelion/Z01Upper`, and the owned Unreal asset is under `ArchitectureKit/Meshes`.

Both views in `Saved/Validation/Aurelion/Z01ReplacementPreview-20260913-185418-69f88598` were inspected before saving. `save_z01_lower_architecture.py` uses the same fit checks and backs up the map before saving. Save evidence is `Saved/Validation/Aurelion/Z01ArchitectureSaved-20260913-185934-fc1d115f/z01-fit.json`; all 1,733 original actor transforms and collision settings were checked unchanged before the save. The review camera is created after saving and is not stored in the map. These are authoring checks, not mission progression or runtime acceptance.

Fresh-load verification passed in `Saved/Validation/Aurelion/Z01SavedReloadCheck-20260913-190132-db47e278/z01-reload-verification.json`: 1,788 actors (1,733 original plus 55 architectural actors), all 54 expected lower-wall placements at unit scale and correct orientation, retained enclosure/proxy collision, hidden original enclosure, and upper-shell bottom at 695 cm. The read-only check left the map clean. Live traversal, combat visibility and performance remain pending.

## Custom Z01 vault and upper end panels

`Art/Source/Aurelion/build_vault_kit.py` adds a four-metre repeating pointed vault spanning approximately 25 metres and a fitted upper end panel. The editable source has fitted ashlar courses, smaller intrados panels, transverse stone ribs with recessed gold rails, longitudinal conductors and stone clasps. It is a structural/surface candidate; repetition, deeper focal detail, material finish and lighting still need art direction work to meet the requested AAA bar.

The vault exports at 32,148 triangles and the end panel at 5,452. Both pass clean-process Blender FBX round-trip checks for dimensions, origins, finite/nondegenerate geometry, three material slots and two UV channels. Unreal import checks dimensions within one centimetre of source bounds, UV channels, source ownership and zero simple collision, assigns the existing owned kit materials, and enables Nanite. The source studio render was inspected. Use Blender `--factory-startup` for these scripts to avoid unrelated installed-addon startup handlers in background runs.

The first unsaved room preview (`Z01VaultPreview-20260913-190629-94704c0c`) exposed a ridge clash against the retained upper enclosure. The corrected preview (`Z01VaultReplacementPreview-20260913-190937-770aa27a`) hides that upper derivative and the old suspended runners, adds two upper end closures, and fits fourteen vault repeats at world Z695 cm. Entry and wall views were inspected. The central roof remains too dark under existing lighting; that is an open visual requirement, not accepted final exposure. The floor, ramps, cover, lower end-wall/door assemblies and other rooms still require replacement.

`save_z01_vault.py` saves the reviewed fit with a pre-save map backup, before creating its temporary camera. It checks all original actor transforms and actor/component collision settings remain unchanged. The new vault/end panels are visual-only; the old physical enclosure remains retained and requires live collision correspondence, traversal and wall-run review. These authoring checks do not qualify gameplay, cinematics, performance or 90% TDD alignment.

Save evidence: `Saved/Validation/Aurelion/Z01VaultSaved-20260913-191240-b3166c65/vault-preview.json`, with the pre-save map backup and inspected saved-room render. Fresh-load checks passed in `Saved/Validation/Aurelion/Z01VaultReloadCheck-20260913-191507-465c77d2/vault-reload-verification.json`: 1,804 actors, fourteen vault repeats and two end panels at the intended unit-scale transforms, replaced upper structures hidden, and expected visibility, no-collision, UV and Nanite settings. The lower-wall verifier also passed. No TDD percentage increase is claimed from this architectural sub-pass.

## Z01 pier-mounted vault lighting

The new `UplightKit` adds a modeled stone-and-gold luminaire with a recessed upward optical aperture, louvers, mounting plate and conductive trim. Six fixtures occupy selected existing pier stations, with six corresponding movable RectLights aimed toward the vault. Each light uses 1,500 lumens, a 2,200 cm attenuation radius and a 45 x 28 cm source. No existing room light is edited. The owned lens material supplies a restrained warm emissive surface; the RectLights provide the architectural illumination.

The Blender fixture render was inspected. Its FBX round-trip passed dimensions, origin, finite/nondegenerate geometry, four material slots and two UV channels. The fixture has 15,484 triangles; this count is recorded for cost tracking, not as proof of quality. Unreal import checks source ownership, dimensions, two UV channels, no simple collision and Nanite, and assigns the four owned materials.

The first 12,000-lumen preview (`Z01UplightPreview-20260913-192023-2bdd10f4`) was rejected for clipped ceiling highlights and excessive floor reflection. A subsequent reduced-output run stopped at the original-state preservation assertion before rendering or saving. The script now writes exact before/after differences while retaining that guard; the failure did not reproduce in `Z01UplightPreservationReview-20260913-192456-cdf50865`, whose difference report is empty. Both the reduced-output entry view and upward detail view were inspected. The vault's stone, ribs and channels now read; shadow noise near bright fixtures, floor reflections, material repetition and final exposure remain open art issues.

`save_z01_uplights.py` backs up M12 and checks every original actor transform and actor/component collision setting before saving the reviewed fit. Its temporary review camera is spawned after saving. Runtime combat visibility, cinematic exposure and the GPU cost of the six added movable lights remain unqualified. This pass does not complete the room or change the evidence-based TDD alignment estimate.

The preservation guard later reproduced its failure during the first save attempt, which stopped without saving. `Z01UplightsSaved-20260913-192715-19cc838d/preservation-differences.json` identifies ten existing sky/space Blueprint actors whose editor billboard components were recreated with numbered names and whose component enumeration order changed. Every actor transform, actor collision flag, component collision value and component count was unchanged after normalizing only those billboard names and ordering. The snapshot now sorts components and retains class/count/collision for non-colliding BillboardComponents while ignoring their generated names. Other component identities, actor transforms and all collision values remain strictly compared; differences are still written before any failed save assertion.

The corrected save passed in `Saved/Validation/Aurelion/Z01UplightsSavedStable-20260913-193002-53a1d805`, with an empty preservation-difference report, pre-save map backup and inspected saved entry render. Fresh-load verification passed in `Saved/Validation/Aurelion/Z01UplightsReloadCheck-20260913-193237-a2c26616/uplight-reload-verification.json`: 1,816 actors, six unit-scale fixtures, six movable lights at 1,500 lumens each, expected positions/radii, and imported UV/Nanite settings. The preceding wall and vault checks also passed. Runtime combat visibility and GPU performance remain unqualified.

## Z01 fitted paving replacement

The `PavingKit` contains a four-metre basalt route tile, a four-metre ivory tile and a three-by-four-metre edge strip. Seven columns and fourteen rows form the complete 26 x 56 metre Z01 floor footprint: 98 unit-scale pieces. The route uses narrow flush gold conductors, small ivory index stones and fitted paving joints. Side paving has restrained alignment keys. The editable Blender cross-section was inspected; all three FBX round-trips passed bounds, origins, material-slot/UV presence and finite/nondegenerate geometry checks. Route tiles have 3,196 triangles; ivory/edge tiles have 2,444 each. These are structural paving candidates, not final AAA surface acceptance.

Owned `M_AurelionKit_PavingIvory` and `M_AurelionKit_PavingBasalt` materials retain textured stone color/normal/roughness inputs with lower contrast and a honed finish. Ivory uses color (0.42,0.40,0.35), 0.12 texture blend and roughness 0.60 + 0.10 times the texture. Basalt uses (0.025,0.028,0.033), 0.06 texture blend and roughness 0.63 + 0.10 times the texture. Both use 0.12 normal blend. Existing wall, floor and kit material assets are not modified by these owned copies. The close-up shows subtle stone variation; surface richness, wear, transitions and final finish remain open art work.

The floor census identified the visible legacy floor below the hidden Z01 floor proxy. In the actual stopped M12, 126 isolated traces confirmed the retained floor at world Z0; every new paving mesh is positioned with its top at that plane. The decorative gold strips at X=-7180/-6820 were collision-enabled and six centimetres above the floor. Their component collision is deliberately retired with their visuals, and 28 additional traces at their old centres verify the flat floor remains. The visible old floor is hidden while its existing collision and the main floor proxy remain. These queries ignore covers and ramps and do not prove live traversal or route clearance.

`Z01PavingPreview-20260913-194013-1e6412ca` established the initial material/assembly view. `Z01PavingCollisionPreview-20260913-194249-e266f42d` passed the 126 floor and 28 retired-strip queries, with an empty unexpected-state-difference report. Entry and paving-detail images were inspected. The remaining broad centre stripe was identified as 52 compressed floor-mesh instances in `Aurelion_Art_M12_Z01_7_e31eb0`. The save recipe checks all 52 belong to Z01, then hides this obsolete non-colliding guide so the new border inlays define the route. Original actor transforms and collision settings are compared against an explicit expected state that permits only the two decorative-strip collision changes.

`save_z01_paving.py` backs up M12 before saving and spawns the review camera afterward. Fresh-load verification covers the complete 98-piece grid, original floor/retired-strip settings and 154 isolated floor-height queries. Runtime footsteps/foot placement, navigation, combat visibility, surface quality and GPU cost remain separate acceptance work. The full M12/M13 rebuild and 90% TDD target are not complete.

The initial saved map's fresh-load check caught the two decorative strips reverting to their original `BlockAll` collision profiles. The in-memory collision-mode change alone was insufficient. `persist_z01_retired_strip_collision.py` recorded both reloaded values, backed up the map, explicitly modified the two components, and saved `NoCollision` profiles. The paving recipe now sets those profiles directly, and its fresh-load verifier requires them. Evidence for the targeted repair is `Z01StripCollisionProfiles-20260913-195245-2eebda0a/retired-strip-profile-save.json`. No other collision profile was changed.

Final fresh-load verification passed in `Saved/Validation/Aurelion/Z01PavingVerifiedReload-20260913-195359-53f1ffc5/paving-reload-verification.json`: 1,914 actors, all 98 paving pieces at the expected unit-scale positions with tops at Z0, hidden legacy surfaces/guides, persistent `NoCollision` on both retired strips, and 154 floor-height queries within tolerance. Wall, vault and uplight checks also passed. The saved entry and paving-detail renders in `Z01PavingSaved-20260913-194644-475f785f` were inspected, including removal of the broad centre stripe. The score remains unchanged pending the full-room/map and runtime acceptance requirements.

## Z01 measured stone bridge replacement

`audit_z01_ramp_and_ends.py` exports the existing ramp/door assets and samples the isolated ramp surface. The completed audit is `Z01RampSurfaceAudit-20260913-200207-4d17a04f/ramp-and-ends.json`; the first audit stopped when a missed trace returned None and the helper was corrected. The original deck reaches approximately 216 cm. Its oversized rails reach approximately 421 cm overall. Door exports are reference evidence for the remaining end-wall pass; those doors have not yet been replaced.

`BridgeKit/measured-profile.json` retains the measured X=-6200 centre-lane heights, transformed into canonical bridge coordinates, with short authored floor-level terminal blends at Y=-16380 and -13090. `build_bridge_kit.py` creates jointed stone wearing courses, a stone deck, layered handrails, intermediate gold rails, collared uprights and side reveals. The replacement is approximately 4.57 m wide and 33.17 m long, with unit-scale placement at (-6164.686518,-14700,0). Its origin is the terminal floor plane. Balustrades are deliberately lowered to 1.1 m above the deck, opening sightlines compared with the old approximately two-metre rails. This is a gameplay-relevant silhouette change that still needs live movement, jumping, cover and encounter review.

The source mesh has 102,648 triangles, three material slots and two UV channels. The stone uses the owned honed paving material alongside kit gold and dark reveals. There are 110 authored convex pieces covering the deck, plinths, handrails, intermediate rails and uprights. Openings are not filled by continuous rail slabs. The measured deck profile is simplified for collision with a 1.5 cm fitting tolerance. The Blender FBX round-trip passed geometry/import checks plus deck/handrail heights and a ray through an open balustrade section. Terminal checks sample 1 mm inside exported boundaries to avoid float rounding at the exact face edge. The Blender assembly render was inspected; fine surface richness, wear, structural refinement and runtime cost remain unqualified.

The first Unreal preview stopped at a validation-code error: `get_simple_collision_count` counts boxes, spheres and capsules separately from convex elements. The engine implementation was inspected and the helper corrected to require zero generated primitives and the authored convex count. `Z01StoneBridgeCollisionPreview-20260913-201659-d99bfd61/bridge-fit.json` then passed 70 deck/handrail surface queries (maximum measured difference about 1.482 cm), a clear balustrade-opening ray, a blocked guard-rail control, and 34 Pawn capsule sweeps along the deck profile. The capsule uses radius 42 cm and half-height 88 cm with 5 cm clearance above the sampled deck. Other actors are ignored: this is isolated geometry evidence, not actual navigation, foot placement or a mission-route pass. Entry and deck-height Unreal images were inspected before saving.

The replacement retires the old ramp's visible mesh and collision profile, preserving its actor transform and other actor settings. The new bridge uses BlockAll with its authored collision; the old ramp explicitly uses NoCollision. `save_z01_bridge.py` checks an expected state permitting only that collision replacement, backs up the map and saves before spawning its temporary review camera. M12/M13 completion, live companion/enemy movement, combat cover, cinematic views and packaged performance remain open. The existing TDD score is unchanged.

The saved-room render in `Z01StoneBridgeSaved-20260913-201947-958c63f2` was inspected. Fresh-load verification passed in `Z01StoneBridgeReloadCheck-20260913-202236-9fdb2eba/bridge-reload-verification.json`: 1,915 actors, the new bridge at the expected unit-scale transform with 110 convex pieces, BlockAll on the new bridge and persistent NoCollision on the hidden old ramp, 70 surface queries, 34 capsule-clearance queries, a clear rail opening and a blocked guard control. The prior wall, vault, light and paving verifiers also passed. This confirms the authored replacement and collision persistence; live gameplay and final art acceptance remain unqualified.

## Z01 fitted end walls and journal door visuals

`audit_z01_portal_gates.py` records the actual gate actor/root transforms separately from their visual components. The original visual doors span approximately 9.75 m wide by 7.22 m high, while the hidden end-wall proxies define a 6 m wide by 4.5 m high passage. Gate actors have nonuniform root scales and much larger blocking boxes (54 m and 81 m wide). These bodies belong to mission journal gates, not static art. The source inspection and native `ASovAurelionJournalGate::Refresh` confirm that the Body and Visual are toggled from the existing TarrikArrival/PressureHall journal facts. The authoring pass must not change or manufacture those facts.

The new `EndwallKit` contains an 8 x 7 m portal fitted around the nominal 6 x 4.5 m proxy opening, a one-metre end return, and a double-faced pair of stone door leaves. Its stone soffit reduces the visible aperture to 6 x 4.2 m to match the retained monolithic collider's approximately 4.224 m intrados. The portal uses coursed jambs, layered collars and entablature, recessed header panels, a journal seal housing and narrow conductive trim. The leaves have carved fields, recessed chevrons, guide rails and central locking teeth. These are editable Blender meshes, not substitutes stretched from another architecture pack. Four existing four-metre kit bays and two one-metre returns flank each portal, covering the hall's 26 m width at unit scale.

The portal has 32,780 triangles, the return 3,008, and the door pair 21,480. All three have three material slots and two UV channels. Clean FBX round-trip verification checks dimensions, origins, finite/nondegenerate geometry and counts; the portal additionally passes nine visual-aperture rays and three frame-hit controls. The studio assembly render was inspected. These checks establish export integrity, not final AAA art acceptance. Surface variation, edge wear, assembly repetition, lighting and runtime cost remain art/performance work.

The fit recipe places fourteen static masonry actors and changes only the two existing gates' visual meshes and child transforms. It inverts root scale on each visual child to place the new leaves at unit world scale without moving or resizing the gate body. It clears legacy material overrides. The original monolithic end-wall mesh and two sci-fi railing spans are hidden visually; their collision is retained. All original actor transforms and collision settings are compared unchanged, and each gate's body transform/extent, collision, mission/beat, blocking direction and visual bindings are checked against `EndwallKit/gate-baseline.json`. The existing instantaneous journal visibility toggle remains; this pass does not claim animated door retraction.

The first preview stopped at an upper-aperture clearance assertion before saving. A subsequent reimport stopped because Unreal's atomic FBX reimport introduced collision into a visual-only asset despite the import option. The shared importer now replaces import settings and explicitly removes collision only when the owned mesh specification requires zero hulls. Positive authored-hull specifications, including the bridge, retain strict count checks. Normal passage probes and the upper-clearance diagnostic are reported separately; no live mission completion is inferred from editor traces.

`Z01EndwallFittedPreview-20260913-204651-80100c54` identified the upper obstruction as the original `aureliondoors` collider at approximately Z422.4 cm, rather than either gate body or the Z450 lintel proxy. The source portal gained a stone soffit at Z420 cm so the visible aperture does not promise the nominal proxy's greater headroom. `Z01EndwallSoffitPreview-20260913-205121-ffb2edc8` then passed eighteen capsule sweeps at three lateral positions and centre heights 93/225/326 cm, six wall/lintel hit controls and two closed-gate controls. Six further probes at centre height 356 cm confirm the retained upper boundary. These ignore other actors and do not substitute for actual traversal, jumping or wall contact.

Both corrected portal views were inspected. The text audit identified the backwards floating label as `Aurelion_Art_Sign_Z01_828f4d`, containing `PRESSURE HALL / AHEAD: SURVIVOR BEND`. The save recipe hides this specific obsolete art sign while preserving encounter hints and journal text behavior. Remaining visual issues include the uneven upper-panel illumination, specular noise, material repetition and final surface treatment. The full M12/M13 rebuild, cast work and runtime acceptance remain incomplete; the evidence-based TDD estimate is unchanged.

The saved southern view in `Z01EndwallsSaved-20260913-205419-ee6dc28b` confirms removal of the floating sign; the run includes a pre-save map backup. Fresh-load verification passed in `Z01EndwallsReloadCheck-20260913-205621-48e43e60/endwall-reload-verification.json`: 1,929 actors, fourteen unit-scale masonry placements, two unit-scale journal visual children with cleared material overrides, unchanged gate authority/bodies, persistent hidden legacy presentation and zero collision on the three imported visual-only meshes. All eighteen clear capsule sweeps, six wall/lintel controls, two closed-gate controls and six upper-boundary diagnostics passed. The previous wall, vault, light, paving and bridge verifiers also passed, and the read-only session left the map clean. This remains an architectural sub-pass, not full-room, mission or AAA quality acceptance.

## Z01 custom combat-cover family

The fresh census in `Z01RemainingPropsAudit-20260913-205845-85f00ac1` found twelve remaining cargo-crate actors, including two scaled to 5.442 m tall. `CoverKit/placement-baseline.json` records their measured transforms, bounds and presentation. The new Blender family replaces these with nine ordinary stone service coffers, one fitted stacked coffer and two authored tall buttresses. All twelve retain their original actor locations and rotations and become unit-scale placements. The cover footprint and top heights match the measured originals; no new actors are added.

`build_cover_kit.py` creates layered footing and lids, recessed access panels, locking straps, fine conductor inlays and separate tall stone courses/flutes. Standard size is 1.434 x 2.796 x 1.123 m; the tall variant is 5.442 m high. The stacked variant adds a 4.2158 cm seating skirt to fill the existing unsupported gap above its lower coffer without changing its top height. The source family has 4,964 / 5,152 / 14,700 triangles, three material slots, two UV channels and one authored convex cover envelope per mesh. The stone uses the owned honed paving material. The envelopes follow overall cover extents; fine recesses and bevels are not separate collision detail.

An initial source round-trip found degenerate faces in extremely thin beveled gold strips. The strips were thickened inward and their negligible bevels removed, preserving module extents. The clean FBX round-trip then passed geometry, dimensions, UV/material counts and authored convex bounds. The source studio render was inspected. Surface wear, unique detail, repetition and final material/lighting quality remain open; triangle counts do not establish the requested AAA bar.

`Z01CoverPreview-20260913-210539-ac1cdcd7` passed sixty cover-top rays, twenty-four blocked side controls, twelve clear above-cover controls and twenty-four side-clearance capsule sweeps. Measured stack seating gap is approximately 0.00009 cm. Both lane and stacked-cover images were inspected. The tests isolate each replacement and do not prove hidden-proxy correspondence, navigation, crouch/peek behavior, companion/enemy traversal or live encounter balance. The replacement explicitly clears old material overrides and uses BlockAll with authored collision. The save recipe backs up M12 and compares all unaffected actor transforms/collision unchanged, saving before creating its review camera. Full-map, cast and runtime acceptance remain unfinished, with no TDD percentage increase claimed.

The saved stack view in `Z01CoverSaved-20260913-210939-ea408138` was inspected. Fresh-load verification passed in `Z01CoverReloadCheck-20260913-211214-c0b482d0/cover-reload-verification.json`: all twelve replacements retain measured position/rotation, footprint and cover-top height at unit scale, with persistent authored collision, correct UV/Nanite settings and no old material overrides. The stacked skirt meets the lower support within tolerance, all isolated collision controls passed again, and every preceding Z01 assembly verifier passed. Actor count remains 1,929 and the read-only session left the map clean. This validates persistence and measured geometry, not final art or runtime acceptance.

## Next room: Survivor Bend survey

`Z02ArchitectureAudit-20260913-211346-85356a31/z02-architecture.json` inventories 87 static-mesh components intersecting Survivor Bend and its approach, with local HISM instance selection. Global sky/enclosure actors can intersect the region and must not be mistaken for room-owned pieces. Both entry and bench-side views were inspected; this first run retained the capture helper's `z01-` filename prefix, subsequently corrected in the script. No map or mission state was saved by the audit.

The hidden Z02 floor is 30 x 18 m at (-7000,-9000), while visible `Cube2` flooring reaches approximately Z7.09 cm. The curved shell is distributed across `Cube_2`/`Cube_3` and upper/end pieces. Broad gold wall surfaces, oversized material patterns, exposed railing spans and mismatched furniture remain replacement candidates. The approach uses `bridge2`, whose bounds include deep supports; its walking surface needs direct sampling before replacement. Preserve the `Aurelion_SecureTarrikRoute` and Selene handoff interaction actors while replacing their visual housings. The next fit must measure actual shell/collision surfaces and passage widths, rather than assuming visible openings match hidden six-metre proxies. This room has been surveyed, not rebuilt.

## Survivor Bend measured paving

`Z02SurfaceMeasurements-20260913-211923-94483de6/z02-surfaces.json` records eight source-mesh exports, 77 paired simple/complex floor probes, 110 lateral shell probes and 22 paired approach probes. The retained floor's visual top is approximately Z7.0917 cm and simple collision top approximately Z7.1714 cm. The approach bridge's sampled simple deck is approximately Z11.1339 cm, around 4 cm above the room floor. These are isolated queries, not actual transition movement or mission qualification.

`Z02PavingKit/floor-fit.json` retains the exact source floor transform and bounds. The replacement covers the full approximately 30.758 x 18.262 m visible footprint, using nine columns and five rows: 45 unit-scale pieces. Existing four-metre route/ivory modules supply the interiors. Six new Blender-authored cut modules cover the asymmetric east/west strips and shorter northern row; no four-metre asset is scaled to force a fit. The world-space basalt route remains centered at X=-7000, with flush gold border inlays. All paving tops match the retained visual floor within tolerance.

The six cut exports pass clean-process geometry, origin, dimensions, material-slot and UV checks. They use the existing owned honed ivory/basalt/gold/reveal materials, two UV channels and Nanite with no collision. The source cut-row render and both room preview views were inspected. `Z02PavingPreview-20260913-212322-4ef39e02` passes 55 retained-floor height queries, including the old decorative-strip locations. The original floor, two strips and fourteen-instance guide are hidden visually. All original actor transforms and collision settings are retained; the strips already lie below the retained floor surface. The save recipe backs up M12 and creates its temporary review camera only after saving. Final surface quality, shell/furniture replacement, interactions, live movement and performance remain open.

The exported inner shell was also inspected in Blender with the perimeter and flat roof hidden for diagnosis. It contains two deep curved side structures beside a flatter central roof, rather than Z01's continuous transverse vault. `z02-arch-intrados.json` in the surface-measurement run records source-only vertical samples at local X=-10 m. The outermost +/-9.5 m samples hit the solid end cap and must not be treated as the open arch profile. The legacy FBX emits missing-layer warnings; this export is geometric reference, not accepted normals/UV source. The shell and bridge have not yet been replaced.

The saved entry render in `Z02PavingSaved-20260913-212722-a64086ad` was inspected. Fresh-load verification passed in `Z02PavingReloadCheck-20260913-213048-f3c9187b/z02-paving-reload-verification.json`: 1,974 actors, all 45 paving placements at the intended unit-scale dimensions and surface height, persistent hidden legacy presentation and retained collision, and all 55 floor probes within tolerance. The preceding Z01 wall, vault, lighting, paving, bridge, end-wall and cover checks also passed. The read-only session left the map clean. Full M12/M13 and 90% TDD acceptance remain unproven and unfinished.

## Survivor Bend custom vault assembly

`build_z02_vault_kit.py` uses the measured inner-shell profile in `Z02VaultKit/measured-profile.json`: approximately 18.204 m open span, 9.695 m crown, and 8.370 m depth per side. The four exported modules provide two-metre vault bays, a fitted closing bay, layered arch faces and a separate coffered central ceiling. Stone courses, recessed ribs, narrow gold conductors and finished arch mouldings replace the minimally detailed inner shell. All placements use unit scale. These are authored production candidates; triangle count and successful imports do not establish AAA quality.

The initial FBX round-trip rejected collapsed bevel faces on clipped ashlar wedges. Removing those wedge bevels resolved the geometry failure. The first Unreal preview (`Z02VaultPreview-20260913-214848-f5a194a9`) exposed a coffer backing in front of the recessed stone surface; the source was corrected, together with the depth of the layered arch moulding. The central ceiling's lowest surface is Z955 cm, approximately 2.4 cm below the retained original ceiling. Decorative intrados offsets still require live collision correspondence review.

The assembly recipe adds thirteen visual-only actors and hides only `Cube_2`, `Cube_4` and `Cube_5`. Their collision stays active, and all original actor transforms and collision modes are compared before/after. Outer wall `Cube_3`, end fillers `Cube_6`/`Cube_7`, windows, furniture and interaction housings remain for subsequent replacement. Neither mission authority nor interaction progression is altered. The broader goal remains a complete custom-kit rebuild across M12 and M13, including transitions and vistas, plus cast and gameplay acceptance; no alignment percentage increase is claimed by this subpass.

Both corrected views in `Z02VaultCorrectedPreview-20260913-215219-df452ab6` and the saved vault detail in `Z02VaultSaved-20260913-215458-3c959013` were inspected. The coffer stone surfaces are visible; a clean-process source regression check also confirms all sixty inset centers expose ivory stone at local Z0.07 m. Fresh-load verification passed in `Z02VaultReloadCheck-20260913-215718-25d4fa4a/z02-vault-reload-verification.json`: 1,987 actors, thirteen correct unit-scale placements, persistent UV/Nanite/no-collision settings, and the three original shell meshes/transforms/collision modes retained with hidden visuals. All preceding Z01 and Z02 paving checks passed, and the read-only session left the map clean. Lighting noise, fine seam treatment, remaining architecture/props, live collision correspondence and GPU cost are still open. No final-art or live gameplay qualification is claimed.

## Survivor Bend perimeter stonework

Inspection of exported `Cube_3`, `Cube_6` and `Cube_7` confirmed two flat side walls and two elevated central end panels. `Z02PerimeterKit/perimeter-fit.json` stores their measured dimensions and original presentation/collision contracts, including the two decorative overhead railing actors. `build_z02_perimeter_kit.py` authors four-metre and 3.3402-metre closing wall bays at the measured 9.64394-metre height, plus a double-faced 12.58812 x 3.85243-metre end frieze. Staggered limestone courses, dressed footings/cornices, narrow conductor inlays and recessed chevron relief replace broad gold sheets and oversized material ornament. Each export has two UV channels and passes the clean FBX geometry checks; the source assembly render was inspected.

The placement recipe uses ten side bays and two end friezes at unit scale. It hides the old perimeter/end meshes and decorative overhead rails while preserving their transforms, assets, collision modes and profiles. Existing windows, columns, furnishings, bridges and interaction housings are still separate follow-up work. New visual wall faces stay within 0.5 cm of the measured original plane, except projecting footings/cornices and fine inlays; those offsets still need live correspondence review. The end friezes retain the measured lower edge around Z587.46 cm. Isolated retained-wall rays and capsules below the end panels supplement preservation checks; they are not a full route, navigation or performance test.

Both preview views in `Z02PerimeterPreview-20260913-220337-4be63bd5` and the saved side-wall view in `Z02PerimeterSaved-20260913-220608-c1d68c10` were inspected. The save passed eight isolated side-wall rays and twelve clear capsules below the end panels, and backed up the map before saving. Fine seam appearance, existing window inserts/columns and furniture are visibly unfinished. The first reload attempt, `Z02PerimeterReloadCheck-20260913-220840-b0b5f50b`, exited with code 3 before loading the map because the local Zen service was unresponsive and the default DDC graph had no writable node. This was not a map verification pass. The runner now has an opt-in `-UseFileSystemCache` switch using UE's configured `InstalledNoZenLocalFallback` graph and a project-drive `DerivedDataCache/ValidationFallback` store. It preserves normal editor defaults and restores the process environment after launch.

The fallback reload passed in `Z02PerimeterReloadFallback-20260913-221057-17b1b489/z02-perimeter-reload-verification.json`: 1,999 actors, twelve unit-scale perimeter placements, eight retained-wall queries and twelve clear isolated end capsules. All prior Z01 and Z02 paving/vault checks passed, with no dirty map left by verification. The same read-only run exported four distinct source meshes and recorded thirteen remaining fittings in `z02-fittings-baseline.json`: four window ornaments, two columns, six table actors and one trim actor. Their physical behavior needs direct inspection before replacement; all report zero simple primitives and convex hulls, which alone does not prove absence of complex collision. The full map/cast/gameplay objective and final AAA visual acceptance remain unfinished; the evidence-supported alignment score is unchanged.

## Survivor Bend reliefs and branching piers

The fittings source inspection confirms the four window-like objects are decorative relief panels against solid walls, while the table mesh includes stools and loose dishes. `Z02GalleryKit/gallery-fit.json` preserves the four relief, two column and one old trim actor contracts. The new custom family uses layered pointed reliefs with nested geometric carving, thin gold fillets and cut stone footings, together with fluted branching piers. The panels retain their main frame position, with a 4.4 cm footing extension meeting the paving. Six new unit-scale actors supply the visuals; seven old visual components, including the trim already covered by the new end frieze, are hidden. Furnishings remain a separate grouped replacement.

The first export found degenerate bevel faces in thin inset geometry. The common path helper now accepts an optional bevel override while preserving previous defaults; the gallery's thin fillets use unbeveled geometry. The corrected exports pass clean-process geometry/UV checks. In `Z02GalleryPreview-20260913-222059-b98fbdc5`, isolated original-column traces missed simple collision while detailed mesh traces hit, exposing a physical defect rather than proving the columns were solid. The new pier therefore has fourteen authored convex pieces: shaft, five collars and eight branch segments. Source tests check both occupied surfaces and open space beneath the forks. The Unreal save recipe tests these again with simple rays and Pawn capsules before writing the map. Original actor assets/transforms/collision settings remain preserved; new pier collision provides the missing simple blocking. This does not qualify live navigation or exact whole-surface correspondence.

`Z02GallerySaved-20260913-222504-f30075ea` stopped at the strict hull-count assertion during Interchange reimport, before the map save; process exit 0 was not treated as a successful script run. The gallery recipe now uses the installed legacy FBX importer for its owned assets, with the same fourteen-hull requirement. `Z02GalleryCollisionSaved-20260913-222747-976d2732` passed twenty occupied/open simple-ray cases and four Pawn capsule controls across both piers before saving. The relief footing check also confirms all four bases meet the paving within 0.05 cm. Fine seam/reflection appearance, live movement/navigation, furnishings and bridge approaches remain open.

The saved gallery detail was inspected. Fresh-load verification passed in `Z02GalleryReloadCheck-20260913-223009-5b3cb067/z02-gallery-reload-verification.json`: 2,005 actors, six fitted placements, persistent fourteen-hull BlockAll piers and visual-only reliefs, all twenty-four authored pier controls, and thirty-six retained-mesh diagnostic rays. All prior Z01 and Z02 assembly checks passed and the read-only session left the map clean. These checks establish saved geometry and isolated blocking, not complete route/navigation or final art acceptance. The full M12/M13 rebuild, supporting cast and 90% TDD objective remain active and incomplete.

## Survivor Bend modular furniture

The exported furniture mesh's surface grid in `Z02PerimeterReloadFallback-20260913-221057-17b1b489/furniture-surface-grid.json` identifies four seats around each table, approximately 45.1 cm seat height and 80.3 cm tabletop height relative to its base. `Z02FurnitureKit/furniture-fit.json` preserves the six original cluster transforms and the measured local seat centers. The new kit separates an octagonal service table from a dressed stone stool, with carved pedestal flutes, recessed fields, small gold inlays and tabletop service objects. Thirty unit-scale actors form six sets on the measured paving surface; no oversized furniture is scaled to force a fit.

Each mesh has three authored convex pieces for its base, support and top. The old combined furniture actors retain their transforms and mesh references but become hidden and explicitly NoCollision, so the old dishes and seats cannot leave invisible detailed-mesh surfaces behind. The new modules use BlockAll; all unaffected actor transforms and collision settings are compared unchanged. Clean FBX checks cover geometry, UVs and top-surface/open-space collision samples. The Unreal fit checks 96 simple surface rays and 60 blocked/clear Pawn capsule controls across the thirty modules. These are isolated authoring controls, not full-room movement, navigation or seating-system qualification. Tabletop service objects remain cosmetic.

Both preview views in `Z02FurniturePreview-20260913-223736-3132ceb2` and the saved detail in `Z02FurnitureSaved-20260913-224009-27433522` were inspected. Fresh-load verification passed in `Z02FurnitureReloadCheck-20260913-224237-f523b61a/z02-furniture-reload-verification.json`: 2,035 actors, thirty unit-scale modules, all 96 surface probes and 60 capsule controls, persistent three-hull BlockAll modules, and all six old furniture actors hidden with collision disabled. The preceding Z01 and Z02 assembly checks also passed and the read-only session left the map clean. Bridge approaches, interaction housings, lighting/seam polish, live movement and the larger M12/M13/cast/TDD objective remain incomplete. No alignment percentage increase is claimed.

## South approach visibility and bridge survey

The bounds-based bridge survey in `audit_z02_approaches.py` includes transformed instance extents, which catches long HISM pieces whose origins lie outside the room. The south approach has two hidden, solid guard volumes spanning Y-11900 to -9900 at X-7288 and -6712, with 22 cm thickness and 130 cm height. Their inner faces leave a 554 cm corridor. The north connector's guards extend 58 metres beyond bridge3; its overlapping railing/floor instances need a complete ownership survey before replacement.

`build_parapet_kit.py` authors a double-faced 4 x 0.22 x 1.3 metre stone parapet with recessed panels, carved borders, dressed footings, hand-rest cornices and narrow gold inlays. Coplanar terminal-pier overlaps were removed after source-render inspection. The FBX round-trip check passes 7,992 triangles, two UV channels and no collision. Ten unit-scale modules visualize the south guard boundaries; the existing guard actors retain collision, transforms, mesh references and visibility settings. All original actor transforms and collision states are compared unchanged before save. The new visual mesh uses Nanite and explicit NoCollision. Twenty-five isolated Pawn controls cover lateral blocking, centre clearance and above-top clearance.

The old bridge decks, outer panels and approximately 50-metre-deep supports still require custom architecture. Source inspection also measured a roughly 6.49 cm mismatch between the old deck's visible and simple collision surfaces. These parapets do not resolve that deck mismatch, the north approach, exterior roof/cap treatment, full-route navigation, lighting or GPU qualification. The overall M12/M13 environment and cast goal remains active; no final AAA-quality or alignment percentage claim is made.

Both preview views in `Z02ApproachGuardPreview-20260913-225941-cf0efee4` and the saved detail in `Z02ApproachGuardSaved-20260913-230134-b895f998` were inspected. Fresh-load verification passed in `Z02ApproachGuardReloadCheck-20260913-230356-f6a99443/z02-approach-guard-reload-verification.json`: 2,045 actors, ten unit-scale visual modules, two retained guard contracts, and all 25 Pawn controls. All preceding Z01/Z02 assembly checks passed; the read-only session left the map clean.

## Custom approach decks and arch supports

`build_approach_bridge_kit.py` replaces the complete `bridge2`/`bridge3` presentation with a full-size, two-part kit. The 14.52963 x 21.29522 metre deck uses 96 fitted wearing courses, narrow route inlays, layered fascia/corbels and double-faced outer stone parapets. The 44,804-triangle deck has three authored convex pieces: a continuous slab and two solid 1.3-metre outer barriers. The deck's visible wearing surface is at local Z11.133914 cm, matching the previously measured old simple-collision height. Fine paving joints expose a backing surface only 4 mm lower; the initial 2 cm backing recess was caught by the source surface check and corrected.

The 48,672-triangle support module has four arch ribs derived from the original underside survey, segmented stonework, shallow horizontal coursing, dressed abutments and transverse ties. Its 180 authored convex pieces cover ribs, abutments and ties while retaining the new open spaces between ribs. These supports descend approximately 49 metres. Both modules have two UV channels, Nanite enabled in Unreal, and explicit BlockAll collision. Clean FBX validation checks deck visual/physical heights and blocked/open regions under the arches. Source renders were reviewed and terminal-pier coplanar overlaps corrected. A slow intermediate coursing build was stopped and replaced with a single batched geometry object; only the completed, verified source is delivered.

Four unit-scale actors replace the two old bridges at their original origins. The old bridge assets/transforms remain intact, with their visuals hidden and collision explicitly disabled. All other actor transforms and collision settings are compared unchanged, including the separate central route guards and north connector HISM pieces. Lowering the outer panels from roughly 2.9 metres to 1.3 metres intentionally changes the peripheral sightline/cover envelope; live combat and traversal qualification remain open. The existing narrow central route remains independently bounded.

The preview `Z02BridgesPreview-20260913-231633-97e03dd4` passed 140 paired simple/complex deck surface queries, 24 arch controls and 30 Pawn controls. Both in-level views were inspected. Distant fine-line aliasing, lighting, exterior roof caps, the wider north connector, live route/navigation and GPU performance remain unfinished. This is environment progress, not a final AAA-quality or 90% TDD acceptance claim.

The save in `Z02BridgesSaved-20260913-231951-b634c3d3` passed the same isolated checks and six whole-editor-world centre-lane probes before/after replacement. Both sets reported the existing `Aurelion_PressureHallExit` gate on all three south lanes and clear north lanes. This preserves the observed gate condition; it does not grant progression facts or establish an unlocked live route. The saved south view was inspected.

Fresh-load verification passed in `Z02BridgesReloadCheck-20260913-232204-5060c450/z02-bridges-reload-verification.json`: 2,049 actors, four correct unit-scale bridge modules, 140 surface probes, 24 arch controls, 30 Pawn controls and the six expected gate/clear centre-lane observations. All fourteen assembly verification receipts passed, with no Python errors or dirty map packages. Full live gameplay and final-art acceptance remain open.

## Survivor Bend exterior closure and roof finish

The current-state overhead survey `Z02RoofAudit-20260913-232525-77da20a5` confirmed broad thin roof plates and unfinished dark side-end faces beside the central friezes. `audit_z02_roof.py` records the overlapping component contracts and two review views without saving the map. The fitted exterior source uses the vault's measured 29.2938 x 19.26214 metre footprint. Twelve wall modules close the four side ends: two four-metre bays and a 0.3697-metre closing pier per wing/end. The central 12.5544-metre span remains open below the existing frieze. The new wall bays begin at the measured paving height, with layered cornices, pilasters, recessed fields and narrow conductor reliefs.

`build_z02_exterior_kit.py` produces the 8,832-triangle four-metre bay, 1,328-triangle closing pier and 17,508-triangle roof finish. The roof adds fitted stone courses, perimeter coping, corner caps and four drainage grates. Each source has two UV channels and one authored convex core. The new roof wearing surface is at world Z999 cm, about 19 cm above the prior central roof top; its collider matches that new surface. Rooftop access/traversal is unqualified, and shallow coping/grate details remain decorative. All existing actors retain their transforms, collision and visibility; this adds thirteen unit-scale actors. Source geometry/convex bounds and rendered end-bay/roof views were reviewed.

The first preview stopped before map save on a complex roof trace. The diagnostic run `Z02ExteriorDiagnostics-20260913-233412-49a92717` measured Z1000.76567 cm where the authored surface is Z999 cm. The asset used Nanite's automatic fallback reduction with relative error 1.0. The owned importer now supports an opt-in `preserve_fallback_geometry` manifest field; this roof requests PercentTriangles at 1.0 and relative error 0.0. The requested setting preserves all fallback triangles, rather than relaxing the surface tolerance. Other assets retain their existing settings. This finding also motivates a future visual/fallback comparison on the noisier large architectural meshes; it does not yet prove the cause of all distant line artifacts.

The corrected preview `Z02ExteriorCorrectedPreview-20260913-233654-756ac895` passed all 54 wall/roof surface controls and ten Pawn passage/blocking controls with the unchanged 3 mm roof-height tolerance. The persisted Nanite fallback settings are asserted by the exterior checker. Both approach and overhead images were inspected: the side-end voids are closed and the roof has a continuous finished perimeter. The south facade remains heavily shadowed, and distant fine-line artifacts remain visible; lighting and broader fallback/rendering analysis are still necessary before final-art acceptance.

The save in `Z02ExteriorSaved-20260913-233954-29700abb` passed and its roof overview was inspected. Fresh-load verification passed in `Z02ExteriorReloadCheck-20260913-234209-cd7c2563/z02-exterior-reload-verification.json`: 2,062 actors, thirteen unit-scale exterior modules, 54 surface checks, ten Pawn controls and persistent full-geometry roof fallback settings. All fifteen assembly receipts passed; no Python errors or dirty map packages remained. The full M12/M13, supporting cast, live gameplay and 90% TDD objective remains incomplete.

## Masonry surface and rendering diagnosis

Same-camera comparisons in `Z02FallbackRenderingCompare-20260913-234651-91b3e1b8`, `Z02NanitePrecisionCorrectedCompare-20260913-235200-df703bd4` and `Z02RenderingModesCompare-20260913-235507-43bc4db4` tested full fallback geometry, 1/64 cm position precision, denser Nanite rendering and disabled shadows. None removed the conspicuous fine-line breakup in the inspected view. The diagnostic scripts restore their temporary settings and save no assets or maps. The first precision run used the wrong sign and is excluded from the fine-precision conclusion; the corrected value follows the installed Nanite encoder's positive quantization scale. No broad Nanite quality/performance change is justified by these captures.

The perimeter source itself used repeated decorative dash marks and nearly black metallic backing just 5 mm behind the limestone. The two wall modules now omit those marks, use warm nonmetallic stone grout, and recess the backing by 45 mm from the unchanged limestone face. `verify_z02_masonry_surfaces.py` checks the limestone and grout depths/materials directly in clean FBX imports. Outer module bounds, original actor transforms, retained collision and all placements remain unchanged. The importer reimports only the two perimeter wall assets; the frieze is unchanged. The normal perimeter import recipe and regression checker now include the dedicated grout slot.

The first asset reimport marked the referencing level dirty and stopped before map save. The revised save path verifies all original transforms/collision, backs up the map, and saves that reimport dirtiness when present. `Z02MasonrySaved-20260914-000340-720156a7` saved the meshes/material and referencing level, and both comparison/detail images were inspected. The close view shows clean construction joints and removal of the repeated perimeter dashes. Fine distant edge breakup, the separate gallery relief's marks, lighting, and the full M12/M13/cast/live-gameplay objective remain unfinished. This refinement is not proof of final AAA quality or a higher TDD alignment score.

Fresh-load verification passed in `Z02MasonryReloadCheck-20260914-000809-2c2b50da`: all fifteen assembly receipts, 2,062 actors, persistent grout material binding and unchanged retained collision. No Python errors or dirty map packages remained.

## Survivor Bend south facade lighting

Six visible `PierUplight` fixtures now support a south-facade lighting recipe in `UplightKit/z02-facade-lighting.json`. Four lights illuminate the exterior wall bays, and two illuminate the central frieze. Their modeled apertures and light sources align; fixtures use unit scale and NoCollision. Movable rectangular lights use a restrained warm color, 45 x 28 cm sources, and bounded attenuation radii. All original actor transforms and collision states are compared unchanged. This adds six fixtures and six light actors without changing global exposure or rendering quality.

The first unsaved preview, `Z02FacadeLightPreview-20260914-001343-e8bb6146`, used 2,400 lumens per wall light and 650 per frieze light. Both views were inspected and rejected for extensive clipping and exaggerated relief shadows. The reduced preview `Z02FacadeLightReducedPreview-20260914-001632-7a4bf6a5` uses 120 and 45 lumens respectively. Both corresponding views were reviewed: the stone fields and thin gold inlays are visible without the first version's broad white clipping. The facade's lower portions remain shadowed, and raking-shadow quality needs further review. These captures do not establish combat visibility, temporal stability or a GPU budget for six additional movable lights.

The reduced lighting saved in `Z02FacadeLightingSaved-20260914-001924-1920c938`; its south-approach image was inspected. Fresh-load verification passed in `Z02FacadeLightingReloadCheck-20260914-002153-6c37ec7e/z02-facade-lighting-reload-verification.json`: 2,074 actors, six unit-scale fixtures, six correctly aimed lights with persistent output/color/radius/source-size settings, and all sixteen assembly verification receipts passed. No Python errors or dirty map packages remained. Live traversal/combat visibility, GPU performance and the full environment/cast/TDD goal remain unqualified.

## North connector parapet replacement

The full north connector survey (`NorthConnectorAudit-20260914-002455-1f60c8f4`) inventories world-space instance bounds across the 58-metre route and the landing. It identified two remaining legacy bridge spans, 62 intersecting railing instances, and 40 intersecting floor instances. The legacy deck spans overlap at their ends; direct reuse of the full-length custom deck would create coplanar surfaces. Fitted bridge ends and the connector floor/landing remain separate unfinished work.

The parapet pass uses fourteen four-metre pieces and one new two-metre closing piece on each side, all at unit scale. The closing module has dressed stone courses, a recessed relief panel, thin gold conductors, two UV channels and 3,432 triangles. Its clean FBX round trip passed. Thirty visual pieces match the existing hidden 22 cm x 130 cm barriers, leaving the same 5.54-metre clear width. Existing barrier collision remains authoritative.

The placement script removes only the 56 doubled railing instances fully contained on this straight crossing. It compares every selected transform against the survey, then compares the complete remaining transform multiset against the original unselected instances. Other actor transforms and collision states must match their original snapshots. Isolated Pawn sweeps sample both barriers, clearance above them, and the central lane at fifteen stations. These checks do not qualify live navigation, combat or frame time.

The first preview stopped on an incorrect importer source directory before map save; the incidental four-metre asset reimport was restored. The corrected `NorthGuardCorrectedPreview-20260914-003312-801f728c` passed, and both approach/detail images were inspected. `NorthGuardsSaved-20260914-003527-d7e5d356` saved the map, with its approach image also inspected. Fresh `NorthGuardsReloadCheck-20260914-003729-cf147417` passed all seventeen assembly receipts: 2,104 actors, thirty parapets, 75 isolated Pawn controls and exact preservation of all 728 unselected railing transforms. No Python errors or dirty map packages remained. The installed railing component is NoCollision; its removal does not replace the retained guard collision. Fine-edge breakup, flat bright paving, the old outer gold panels and legacy bridge supports are visible outstanding art issues. This is incremental architecture progress, not final AAA acceptance or evidence that the 90% TDD target has been reached.

## Fitted north bridge spans

The north connector's two remaining legacy spans overlap by roughly 0.9 m at their ends. The new `NorthBridgeKit` fits span A between the existing custom bridge3 deck's north plane and the midpoint between legacy bridge4/bridge5; span B continues from that plane to the original bridge5 north edge. Their lengths are 19.97667537 m and 20.86340535 m. All instances use unit scale. The existing approach builder now accepts explicit output folder, mesh prefix and span length, retaining its original defaults; it refits the sampled arch curve proportionately and keeps the 14.52963488 m deck width and 11.133914 cm wearing-course height.

Each span has a dedicated deck and four-rib arch support mesh, with paving courses, narrow conductor inlays, dressed parapets, layered fascia, corbels and stone coursing. Each deck has three authored convex hulls; each support set has 180. Both have two UV channels and pass clean FBX round-trip checks. The new placement recipe replaces legacy bridge4/bridge5 visibility and collision, preserves their transforms and original mesh references, and checks all unrelated actor transforms/collision unchanged. Whole-world probes cross the two shared deck end planes, while isolated tests check physical surfaces, arch openings and parapet clearance. These are geometry checks, not live traversal or performance acceptance.

`NorthBridgePreview-20260914-004257-dc415b02` passed 140 surface probes, 24 arch controls, 30 isolated Pawn controls, six whole-world span-lane controls and six shared-end capsule sweeps. Both preview images were inspected. `NorthBridgesSaved-20260914-004614-8e3b9c7e` saved the four replacement actors; the wider exterior view shows all three custom spans together. Fresh `NorthBridgesReloadCheck-20260914-004943-7bc95409` passed all eighteen assembly receipts, 2,108 actors and the same physical controls, with no Python errors or dirty map packages. Legacy bridge4/bridge5 gold panels are replaced by the lower stone parapets; their changed peripheral sightlines still require live combat assessment. Distant coursing artifacts and bright, visually flat paving remain fidelity issues.

The unsaved `NorthBridgeNaniteCompare-20260914-005106-d4ce572a` compared these six bridge asset types with Nanite enabled against Nanite disabled and full conventional geometry. Both images were inspected. The visible coursing breakup persisted, so this experiment does not justify disabling Nanite. All six asset settings were restored and no map or asset was saved. The next surface pass should reconsider fine construction detail and material presentation; this comparison does not prove a single root cause or final visual acceptance.

## Bridge masonry surface rebuild

The source-level surface pass replaces fifty-metre segmented strips and thin dark overlay lines with staggered two-metre ashlar courses over a continuous, nonmetallic mortar core. Course gaps are 36 mm; the mortar face is recessed 50 mm from the dressed stone face. Blocks follow the existing sampled arch profile and retain the separate authored collision hulls. The foot blocks use sharp edges where beveling tiny clipped polygons collapsed faces in the FBX round trip; the first two candidates failed that check, and the corrected approach support passed it before proceeding to the fitted north variants.

`verify_bridge_ashlar.py` checks stone/mortar hit depth and material assignment in clean FBX imports. It also compares sorted hull-vertex signatures against the committed `b831e4e3` FBX exports, with the baseline revision recorded in `NorthBridgeKit/ashlar-collision-baseline.json`. Reimport is scoped to the three support asset types; deck meshes and all actor placements are retained. The normal bridge import recipes and the saved-assembly checker now recognize the dedicated StoneGrout material slot.

All three corrected support FBXs passed clean round trips; stone/mortar rays passed, and all 180 hull signatures per asset matched the committed baseline. The first Unreal reimport saved the mesh assets but stopped before map save on a Python material-slot accessor; the checker now uses `get_editor_property`, matching the established importer/perimeter checks. `BridgeAshlarSaved-20260914-010910-6b34eed3` saved the three assets and referencing map, with unchanged actor transforms/collision. Both distant comparison and close arch images were inspected. Staggered courses read more coherently and replace the dense vertical-strip pattern, but distant edge/shadow breakup, the separate deck fascia/parapet details and overall lighting remain unfinished.

The capture recorded screen percentage 100, TSR method 4, anti-aliasing quality 3, TSR history percentage 200 and high-resolution screenshot delay 4. No global rendering settings were changed. Support triangle counts are now 153,520 (Approach), 138,736 (North A) and 155,920 (North B); this additional visual geometry has not been qualified against a packaged GPU budget. Source and geometry checks do not establish final AAA quality, live navigation/combat acceptance or a higher TDD score.

Fresh `BridgeAshlarReloadCheck-20260914-011151-b9d4c4d3` passed all eighteen assembly receipts with 2,108 actors, persistent StoneGrout bindings, physical surfaces/arch openings and clear bridge joints. No Python errors or dirty map packages remained. The complete environment, supporting cast and 90% TDD goal remains active.
