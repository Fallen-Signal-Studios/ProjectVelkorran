# Remaining engineering audit and implementation

Subsequent source delivery: [Source engineering closure and adversarial-audit handoff](SourceEngineeringClosure-2026-09-05.md) supersedes the remaining-work list below. This report remains the historical PR #28 baseline and its recorded validation is not rerun engine evidence.

5 September 2026. Baseline: `144544d5d97fa4a3c1720b8420dba822a961bce1`, delivered in PR #27 and merged into the integration branch with identical tree `8f8062a39bf8875fefb31eadd5b6fdef652e6457`. This report supersedes the remaining-work descriptions in `TDDNativeCompletion.md`.

## Authority and evidence

Authority remains **Sovereign Call: Origins TDD v2.0, 14 August 2026**, plus `CampaignV2ChangeLog.md`. The attached December 2025 document is superseded. Approved protagonist weapon rosters, player Health recharge, finite Cinderline ammunition, deterministic primary damage and transient combat sustain remain intact. No free protagonist switching, classes, randomized loot, crafting or co-op is introduced.

The previous delivery supplied substantial native combat, campaign, interaction and recovery code. This pass inspected its residual integrations and found additional defects in actual serialization, map-load callbacks, enemy acquisition, deferred crowd state and cinematic cleanup. Source implementation and portable checks do not establish Unreal compilation or an end-to-end playable campaign.

This checkout has no project/Narrative `.uasset` or `.umap` content. UE5.7, UHT/UBT, required engine tools and ZenDyn are unavailable. Editor authoring is excluded at the creator's request. Missing native frontend/platform consumers are still counted as engineering work.

## Gap decisions

`P` means `Source/ProjectVelkorran`. `N`, `UI` and `S` mean the existing NarrativeArsenal, NarrativeCommonUI and NarrativeSaveSystem modules under `Plugins/Narrativeed3f9374a6eV6/Source`. Every change extends an existing owner or adds a consumer of that owner.

| Order / issue | What existed; TDD requirement and actual defect | Disposition and implemented change | Dependencies and risk |
| --- | --- | --- | --- |
| 1. Save-load completion | Slot validation/travel existed. Initial restore failure could retain ownership or wait for timeout; same-map requests had no unique callback identity. §§11.8–11.9,15.16 require explicit recovery without a replacement campaign. | **Refactor** `P/Save/SovSaveSubsystem`: exact URL request token, stale-world rejection, detection of actual Narrative initialization failure, single terminal notification and ownership release before listeners. A rejected world stays blocked; a deliberate fresh no-slot world can start. | GameInstance, Narrative initial-load delegate, managed readiness. **High**: stale callbacks, duplicate completion, stranded recovery. |
| 2. Serialization correctness | Identity transform doubled as an omission sentinel. Stable-ID-only actors could produce invalid save records. §§11.8,15.9–15.11 require exact transforms and identities. | **Extend** `S/NarrativeSave` with explicit transform presence and legacy fallback; clear the flag at intentional controller/PlayerState suppression sites. Capture world records only for savable owners while retaining stable-only lookup and duplicate-ID rejection. Root recovery candidates during asset loads; preserve misfiled bank data before reuse; treat local user-index changes as account ownership changes. | Existing tagged serializer, actor interfaces and two-bank storage. **High**: displaced actors, invalid snapshots, unintended account namespace changes. |
| 3. Finite enemy knowledge | Narrative perception/activities and A/B quotas existed; source/confidence/expiry was incomplete and native abilities could independently reacquire targets. §§8.5–8.6 require bounded knowledge, sharing and investigation. | **Extend** `N/AI/NarrativeNPCController` with source/strength/confidence/position/expiry, bounded faction sharing and concealment rules. Native selection, deliberate Hound/Drone targeting and rocket homing consume the same query. Director restoration and future waves acquire separate suspension leases. Collateral damage remains physical. | AI Perception, actual sensors/goal generators, blackboard ownership and encounter state. **High**: wall tracking, blind legacy controllers, stale-focus callbacks and hidden-wave information leaks. |
| 4. Deferred crowd state | Existing Narrative Mass pedestrians used a raw actor in deferred representation work; an opposite request could be skipped before the queue flushed. | **Refactor** existing Mass representation updates with weak lifetime/association checks and deferred state reconciliation. Keep the existing Mass pipeline. Full campaign-combatant C/D migration remains unimplemented. | Existing entity/actor association and deferred execution. **High**: destruction, pooling/reassignment and disable/enable ordering. |
| 5. Ballistic assistance | Assistance covered zero-gravity Hunger; Cinder/Stillpoint lacked moving-target gravity interception. Stillpoint used hard-coded gravity and frame-dependent integration. §§6.9–6.10 require bounded assistance and consistent physical projectiles. | **Extend** existing aim policy and actual Cinder, Stillpoint and Hunger releases: fixed speed, eight-degree/0.6-second bounds, range/fuse/speed limits and swept curved-path clearance. Stillpoint uses world gravity and constant acceleration. Rejected assistance preserves the original launch. | Real projectile profiles, collision and settings. **Medium/high**: impossible arcs, cover, changing motion or custom Blueprint movement. |
| 6. Native frontend behavior | Activation bypassed CommonUI's focus cache; menu wrap and consistent native accessible button names were missing. §13.9 requires focus memory, navigation and semantics. | **Extend** `UI/NarrativeActivatableWidget`, common buttons and `N/Widgets/NarrativeMenu`: routed focus refresh, activation-generation guards, owned boundary wrap and localized full-button names on the actual Slate button. | Actual CommonUI layers/widgets, Slate and platform accessibility. **Medium/high**: modal focus theft, removed controls and authored navigation overrides. Reader/timed-choice integration remains open. |
| 7. Haptic and HDR output | General settings existed without a channel router/native producers or transactional HDR preview. §13.9 requires configurable supported output. | **Extend** controller/settings ownership with bounded semantic haptic channels and actual damage, admitted interaction and sequence producers. HDR uses engine capability/output APIs with receipt-scoped preview, timeout, confirmation and rollback. Unrelated settings saves cannot confirm a preview, including device loss. | Local controller, Narrative events, UE APIs and devices. **High**: lingering output and unconfirmed settings persistence. Complete display calibration and platform services remain open. |
| 8. Partitioned cinematics | Managed scenes loaded assets/classic levels but did not request partition cells; participants could be resolved before streaming. Final writes lacked native world mechanisms. §§11.10,14.13 require readiness and equivalent playback/skip results. | **Extend** the existing cinematic component with owned Activated-cell regions, streamed actor witnesses, deferred binding, watchdog and exact teardown. Typed door/lift power/lock postconditions use existing setters with revisions to preserve later same-value writes. Fix protected-field accesses, checkpoint replay and unregistration cases. | Actual partition maps/Data Layers, scene IDs, native transit and journal. **High**: empty readiness proofs, memory pressure, lost ticks and stale rollback. Inventory mutation remains outside this contract. |
| 9. Automation completeness | The Windows runner could accept a successful report omitting newly registered suites. §18 requires evidence for the changed systems. | **Extend** the existing runner with `Scripts/Check-UnrealReport.py`: inventory selected native simple tests across project/plugins; reject missing/duplicate/incomplete rows, inconsistent totals and error events. Retain `coverage.json`. | Python 3 and a real built editor/report. **Medium**: coverage cannot detect same-name stale implementations; the default build gate remains required. |

## Vertical slices and acceptance

| Slice / owners | Implementation work | Testing and likely failures | Definition of done |
| --- | --- | --- | --- |
| 1. Resume safely: `SovSaveSubsystem`, Narrative record/serializer, controller/PlayerState/director suppression | Exact load requests, terminal recovery, transform/identity correctness, last-good retention | Actual serializer/load-world UE regressions; 100 storage-fault/recovery iterations over retained bytes; stale URL, missing actor, denied/torn writes and subsystem restart. Packaged process-kill/travel remain required. | A failed load reports recovery once, preserves prior data and cannot silently start a replacement campaign; later explicit recovery/new game remains possible. |
| 2. Fight finite-knowledge enemies: Narrative controller/threat policy, bot selection, Hound/Drone/rocket, director/coordination, existing Mass adapter | Real acquisition/steering gates, finite investigation, separate suspension owners, safe deferred representation | Production decay/sharing tests and UE perception/direct GAS/blackboard/suspension/Mass regressions; concealment, lost sensors, legacy receivers, possession callbacks, future waves and pooled actors. | Deliberate attacks use legitimate current knowledge; staging/retry reveal no current positions; collateral hits still follow normal combat rules. |
| 3. Throw consistently: `SovAimAssist`, native release methods, `SovSeleneCombatProjectile` | Gravity interception and actual launch integration | Production numerical tests and UE GAS/physics regressions; low/high arcs, no solution, range/fuse limits, cover and 30/60/120 Hz. | Assistance stays inside weapon and geometry limits; rejection preserves unassisted behavior and resource ownership. |
| 4. Operate menus/output: Narrative CommonUI, `SovGameUserSettings`, `Feedback/*`, controller component | Existing focus restoration/wrap, button semantics, channel output and HDR preview | UE Slate/output adapter tests, then real menus/devices; callback reactivation, authored navigation, pause/death/sequence cleanup, unsupported output and device loss followed by save. | Native controls retain valid focus/semantics, feedback respects settings and stops, and unconfirmed HDR never becomes a saved preference. |
| 5. Enter/leave partitioned scenes: cinematic component/policy and native transit revisions | Cell preparation, watchdog, deferred participants and shared completion/skip postconditions | Production policy plus UE lifecycle/postcondition regressions, then a cooked partition map; absent witnesses, paused timeout, lost ticking, unregistration, rollback damage and same-value rewrites. | Playback waits for required activated content; finish/allowed skip agree; failure retires owned resources and preserves later world writes. |
| 6. Reject misleading validation: report checker and existing Windows runner | Source-to-report coverage and retained evidence | Negative tests against the production checker using explicitly synthetic reports; omitted tests/modules, duplicate rows, bad counters and hidden errors. Then execute the real UE runner. | Every required source registration appears as successful; missing engine/process/report evidence cannot become a pass. |

## Remaining engineering and next order

1. **Real UE5.7 integration.** Run UHT/UBT, all automation, Blueprint compilation and shipping mission validation; fix resulting native/API/link errors. Public Epic API pages informed implementation but may show a newer version. Actual 5.7 headers/compiler remain authoritative.
2. **Packaged campaign/recovery and performance.** Validate M01/M02 protected facts and Lyric extraction, plus M12/M13 dual-hero state. Run real legal-boundary loads, 100 death/retry cycles, process termination during writes, partition traversal, long sessions and frame/memory captures. The storage stress test is not a packaged death-loop or process-kill certification.
3. **Remaining frontend/platform consumers.** Implement timed-choice presentation together with actual reader completion in Tales/CommonUI. Evidence/dialogue semantics, high contrast/outline/subtitle consumers and first-boot accessibility remain. Add approved account callbacks/cloud-conflict recovery, controller audio/proprietary haptics, complete HDR black-floor/paper-white/UI-luminance calibration and output-device identity. An external OS move between displays with identical HDR values is not yet distinguished by the preview receipt.
4. **Campaign Mass C/D migration.** Pedestrian archetype/seed representation does not preserve campaign identity, GAS, sever/injury, required participation or snapshots. Implement state transfer/rehydration through existing encounter/Narrative authority and test identity/performance. The deferred-lifetime fix does not close this gap.
5. **Richer cinematic and custom native adapters.** Inventory grants/removals need authoritative Narrative inventory/progression transactions and committed-beat rules. Arbitrary Sequencer Blueprint side effects cannot be rolled back generically. Custom projectile movement, custom AI goals and direct third-party feedback need their real integration paths.
6. **Future shipped formats.** External campaign schema remains 1.0. Future changes require immutable historical fixtures and deterministic migrations; no historical shipped v2 format is fabricated.

Next three slices: (1) complete the real UE build/regression gate; (2) automate packaged opening/convergence recovery and partition traversal; (3) complete accessible frontend and approved platform adapters. Each requires engine/content or device evidence unavailable here. Maps, mission/encounter assets, animation, UI layout and perceptual authoring remain excluded from this request.

Detailed contracts: [save](SaveSlotEngineering.md), [threat memory](ThreatMemoryEngineering.md), [ballistics](BallisticAssistEngineering.md), [frontend](FrontendAccessibilityEngineering.md), [output](PlatformOutputEngineering.md), [cinematics](PartitionedCinematicEngineering.md), [validation](UnrealValidation.md). Final executed checks and the file inventory are in `TDDRemainingEngineeringValidation.md`.
